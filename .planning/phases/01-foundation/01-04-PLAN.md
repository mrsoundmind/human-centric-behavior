---
phase: 01-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/experiences/DelayedResponseExperience.tsx
  - src/components/experiences/internal/RoleJourney.tsx
  - src/components/experiences/internal/sales/LeadGenFriction.tsx
  - src/components/experiences/internal/sales/Stage1_LeadGen.tsx
  - src/components/experiences/internal/sales/Stage2_Meeting.tsx
  - src/components/experiences/internal/sales/Stage3_Proposal.tsx
  - src/components/experiences/internal/sales/Stage4_Closing.tsx
  - src/components/experiences/internal/sales/Stage5_Handover.tsx
  - src/components/experiences/layer2/SDLCContext.tsx
autonomous: true
requirements: [TECH-01, TECH-02]

must_haves:
  truths:
    - "No console warnings about unmounted component state updates when navigating in/out of DelayedResponseExperience rapidly"
    - "Event listeners are cleaned up on unmount in DelayedResponseExperience — no duplicate handlers fire"
    - "GradientBackground mousemove listener is attached in useEffect with cleanup — no multiple listeners on remount"
    - "All 6 sales stage onComplete callbacks have typed data parameters instead of any"
    - "localStorage access in GlobalExperienceContext is wrapped in try/catch"
  artifacts:
    - path: "src/components/experiences/DelayedResponseExperience.tsx"
      provides: "Fixed memory leaks — useRef for waitStartTime, proper event listener cleanup"
    - path: "src/components/experiences/internal/RoleJourney.tsx"
      provides: "GradientBackground mousemove listener in useEffect with cleanup"
    - path: "src/components/experiences/internal/sales/LeadGenFriction.tsx"
      provides: "Typed onComplete callback"
  key_links:
    - from: "src/components/experiences/DelayedResponseExperience.tsx"
      to: "document event listeners"
      via: "useEffect with cleanup return function"
      pattern: "removeEventListener"
    - from: "src/components/experiences/internal/RoleJourney.tsx"
      to: "window mousemove"
      via: "useEffect with cleanup return function"
      pattern: "removeEventListener.*mousemove"
---

<objective>
Fix all technical health issues catalogued in CONCERNS.md: memory leaks from unmanaged event listeners and timers, stale closure in DelayedResponseExperience, GradientBackground mousemove leak, unguarded localStorage access, and `any` type usage in sales stage callbacks.

Purpose: These fixes prevent runtime bugs (duplicate event handlers, stale closures, localStorage crashes in privacy mode) and improve type safety in existing code. Phase 1 requires resolving all pre-existing concerns before downstream phases add complexity.

Output: 9 existing files fixed — no new files created.
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix memory leaks — event listeners, timers, stale closures</name>
  <files>src/components/experiences/DelayedResponseExperience.tsx, src/components/experiences/internal/RoleJourney.tsx</files>
  <read_first>
    - src/components/experiences/DelayedResponseExperience.tsx (full file — understand all useEffect hooks, event listeners, setTimeout calls, and the waitStartTime stale closure bug)
    - src/components/experiences/internal/RoleJourney.tsx (full file — find GradientBackground component and its mousemove addEventListener)
    - .planning/codebase/CONCERNS.md (Memory Leak Risks section — lines 39-61, Known Bugs section — lines 254-269)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pitfall 3: stale closure fix, Pitfall 4: mousemove listener fix, useEffect cleanup code example — lines 537-557)
  </read_first>
  <action>
**Fix 1: DelayedResponseExperience.tsx — stale closure**

Find the `waitStartTime` state variable (likely `const [waitStartTime, setWaitStartTime] = useState<number | null>(null)`). Replace with:
```typescript
const waitStartTimeRef = useRef<number | null>(null);
```
Update all references:
- Where `setWaitStartTime(Date.now())` is called, replace with `waitStartTimeRef.current = Date.now()`
- Where `waitStartTime` is read (in setTimeout callbacks), replace with `waitStartTimeRef.current`
- Remove `waitStartTime` from any dependency arrays (refs don't need deps)

**Fix 2: DelayedResponseExperience.tsx — event listener cleanup**

Find all `useEffect` hooks that call `document.addEventListener("click")` and `document.addEventListener("keydown")`. Ensure each has a cleanup function that:
1. Calls `document.removeEventListener` with the SAME handler reference
2. Clears ALL `setTimeout` IDs stored in the effect

Pattern to apply (adapting to existing code structure):
```typescript
useEffect(() => {
  if (!isActive) return; // guard — only attach when interaction phase is active

  const handleClick = (e: MouseEvent) => { /* existing handler logic */ };
  const handleKeyDown = (e: KeyboardEvent) => { /* existing handler logic */ };

  document.addEventListener("click", handleClick);
  document.addEventListener("keydown", handleKeyDown);

  const timers: ReturnType<typeof setTimeout>[] = [];
  // Wrap each setTimeout: timers.push(setTimeout(...))
  // Any existing setTimeout calls within this effect should push to timers array

  return () => {
    document.removeEventListener("click", handleClick);
    document.removeEventListener("keydown", handleKeyDown);
    timers.forEach(clearTimeout);
  };
}, [isActive]); // adjust deps to match existing code
```

If event handlers are defined OUTSIDE the useEffect (in the component body), move them INSIDE the useEffect so the cleanup function captures the same reference.

**Fix 3: RoleJourney.tsx — GradientBackground mousemove**

Find the `GradientBackground` component (or subcomponent) that has `window.addEventListener("mousemove", ...)`. Wrap it in a useEffect with cleanup:

```typescript
useEffect(() => {
  const handleMouseMove = (e: MouseEvent) => {
    // existing mousemove logic — update gradient position
  };

  window.addEventListener("mousemove", handleMouseMove);

  return () => {
    window.removeEventListener("mousemove", handleMouseMove);
  };
}, []); // empty deps — attach once, clean up on unmount
```

If the existing code defines the handler inline in `addEventListener`, extract it to a named function so `removeEventListener` can reference it.

Also check for any `setTimeout` calls in this file and ensure they have `clearTimeout` in cleanup.

**IMPORTANT:** Do NOT change the visual behavior or logic of these components. Only fix the memory leak patterns. The user-facing experience should be identical.
  </action>
  <verify>
    <automated>cd "/Users/shashankrai/Documents/Duplicate human-centric-lab-main 15 jan" && npx tsc --noEmit 2>&1 | head -20 && grep -c "removeEventListener" src/components/experiences/DelayedResponseExperience.tsx && grep -c "removeEventListener" src/components/experiences/internal/RoleJourney.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/experiences/DelayedResponseExperience.tsx contains `useRef` for waitStartTime (not useState)
    - src/components/experiences/DelayedResponseExperience.tsx contains `waitStartTimeRef.current`
    - src/components/experiences/DelayedResponseExperience.tsx does NOT contain `setWaitStartTime` (useState setter removed)
    - src/components/experiences/DelayedResponseExperience.tsx contains `removeEventListener("click"`
    - src/components/experiences/DelayedResponseExperience.tsx contains `removeEventListener("keydown"`
    - src/components/experiences/DelayedResponseExperience.tsx contains `clearTimeout` in useEffect cleanup
    - src/components/experiences/internal/RoleJourney.tsx contains `removeEventListener("mousemove"`
    - src/components/experiences/internal/RoleJourney.tsx contains mousemove handler inside `useEffect`
    - TypeScript compilation succeeds (npx tsc --noEmit exits 0)
  </acceptance_criteria>
  <done>Stale closure fixed via useRef. All document event listeners have matching removeEventListener in cleanup. All setTimeout calls tracked and cleared on unmount. GradientBackground mousemove listener attached in useEffect with cleanup. No behavior changes — only memory leak fixes.</done>
</task>

<task type="auto">
  <name>Task 2: Type sales stage callbacks and guard localStorage access</name>
  <files>src/components/experiences/internal/sales/LeadGenFriction.tsx, src/components/experiences/internal/sales/Stage1_LeadGen.tsx, src/components/experiences/internal/sales/Stage2_Meeting.tsx, src/components/experiences/internal/sales/Stage3_Proposal.tsx, src/components/experiences/internal/sales/Stage4_Closing.tsx, src/components/experiences/internal/sales/Stage5_Handover.tsx, src/components/experiences/layer2/SDLCContext.tsx</files>
  <read_first>
    - src/components/experiences/internal/sales/LeadGenFriction.tsx (find onComplete prop typed as `(data: any) => void`)
    - src/components/experiences/internal/sales/Stage1_LeadGen.tsx (find onComplete prop typed as `(data: any) => void`)
    - src/components/experiences/internal/sales/Stage2_Meeting.tsx (find onComplete prop)
    - src/components/experiences/internal/sales/Stage3_Proposal.tsx (find onComplete prop)
    - src/components/experiences/internal/sales/Stage4_Closing.tsx (find onComplete prop)
    - src/components/experiences/internal/sales/Stage5_Handover.tsx (find onComplete prop)
    - src/components/experiences/layer2/SDLCContext.tsx (find unguarded localStorage access and `designConfiguration: any`)
    - src/components/experiences/GlobalExperienceContext.tsx (find localStorage access — wrap in try/catch)
    - .planning/codebase/CONCERNS.md (Type Safety Issues section lines 19-29, localStorage section lines 139-144)
  </read_first>
  <action>
**Fix 1: Type sales stage onComplete callbacks**

For each of the 6 sales stage files, read the file and identify what data the `onComplete` callback actually passes. Then:

1. Define a specific interface for each stage's completion data. Name them:
   - `LeadGenFrictionCompletionData` (in LeadGenFriction.tsx)
   - `LeadGenCompletionData` (in Stage1_LeadGen.tsx)
   - `MeetingCompletionData` (in Stage2_Meeting.tsx)
   - `ProposalCompletionData` (in Stage3_Proposal.tsx)
   - `ClosingCompletionData` (in Stage4_Closing.tsx)
   - `HandoverCompletionData` (in Stage5_Handover.tsx)

2. Each interface should have fields matching what the component actually passes to `onComplete(...)`. Read each file's `onComplete` call to determine the exact shape. Common fields will likely include things like: `decisionsCount`, `outcome`, `timeSpent`, `choicesMade: string[]`, etc. Use the ACTUAL data being passed — do not invent fields.

3. Replace the props interface in each file:
   - Before: `onComplete: (data: any) => void`
   - After: `onComplete: (data: LeadGenCompletionData) => void` (using the appropriate type)

4. Export each completion data interface so parent components can use it.

**Fix 2: Guard localStorage access**

In `SDLCContext.tsx`, find any direct `localStorage.getItem()` or `localStorage.setItem()` calls. Wrap each in try/catch:
```typescript
// Before:
const saved = localStorage.getItem("hcl_max_unlocked_layer");

// After:
let saved: string | null = null;
try {
  saved = localStorage.getItem("hcl_max_unlocked_layer");
} catch {
  // Privacy mode or quota exceeded — use default
}
```

Do the same in `GlobalExperienceContext.tsx` if it has direct localStorage access.

For `SDLCContext.tsx`, also note: the `designConfiguration: any` type annotation (line 40, 72) is acceptable in Phase 1 per CONTEXT.md locked decision "Enable stricter TypeScript for new files only (don't break existing code in this phase)". Do NOT change this — it risks breaking existing code.

**IMPORTANT:** Do NOT change any component behavior or visual output. Only change type annotations and add try/catch guards.
  </action>
  <verify>
    <automated>cd "/Users/shashankrai/Documents/Duplicate human-centric-lab-main 15 jan" && npx tsc --noEmit 2>&1 | head -20 && grep -c "data: any" src/components/experiences/internal/sales/*.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "data: any" src/components/experiences/internal/sales/LeadGenFriction.tsx` returns 0
    - `grep -c "data: any" src/components/experiences/internal/sales/Stage1_LeadGen.tsx` returns 0
    - `grep -c "data: any" src/components/experiences/internal/sales/Stage2_Meeting.tsx` returns 0
    - `grep -c "data: any" src/components/experiences/internal/sales/Stage3_Proposal.tsx` returns 0
    - `grep -c "data: any" src/components/experiences/internal/sales/Stage4_Closing.tsx` returns 0
    - `grep -c "data: any" src/components/experiences/internal/sales/Stage5_Handover.tsx` returns 0
    - Each sales file contains `export interface` with a `CompletionData` suffix
    - src/components/experiences/layer2/SDLCContext.tsx contains `try` and `catch` around localStorage calls
    - src/components/experiences/layer2/SDLCContext.tsx still contains `designConfiguration: any` (not changed — locked decision)
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>All 6 sales stage files have typed onComplete callbacks with specific interfaces. localStorage access in SDLCContext.tsx and GlobalExperienceContext.tsx wrapped in try/catch. No behavior changes — only type safety and error handling improvements.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `grep -rn "data: any" src/components/experiences/internal/sales/` returns 0 matches
3. `grep -c "removeEventListener" src/components/experiences/DelayedResponseExperience.tsx` shows cleanup exists
4. `grep "useRef" src/components/experiences/DelayedResponseExperience.tsx` shows waitStartTimeRef
5. `grep "try" src/components/experiences/layer2/SDLCContext.tsx` shows localStorage guard
6. `npm run dev` starts without errors — no runtime regressions
</verification>

<success_criteria>
- DelayedResponseExperience: waitStartTime uses useRef, event listeners have cleanup, timers cleared on unmount
- RoleJourney GradientBackground: mousemove listener in useEffect with cleanup
- All 6 sales stages: onComplete typed with specific interfaces (no `any`)
- SDLCContext + GlobalExperienceContext: localStorage wrapped in try/catch
- TypeScript compiles clean
- No visual or behavioral changes to any component
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
