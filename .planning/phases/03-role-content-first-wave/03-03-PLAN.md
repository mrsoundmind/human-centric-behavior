---
phase: 03-role-content-first-wave
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - data/scenarios/qa/discovery.ts
  - data/scenarios/qa/requirements.ts
  - data/scenarios/qa/design.ts
autonomous: true
requirements: [ROLE-01, ROLE-02, ROLE-03, ROLE-04, ROLE-05, ROLE-06, BEHV-01]

must_haves:
  truths:
    - "QA Discovery phase has 4 scenarios covering all 4 friction dimensions"
    - "QA Requirements phase has 4 scenarios covering all 4 friction dimensions"
    - "QA Design phase has 4 scenarios covering all 4 friction dimensions"
    - "Every QA scenario debrief has a tomorrowAction naming a specific workflow artifact"
    - "All 12 QA scenarios pass Zod validation at import time without errors"
  artifacts:
    - path: "data/scenarios/qa/discovery.ts"
      provides: "4 validated QA Discovery scenarios"
      exports: ["qaDiscoveryScenarios"]
    - path: "data/scenarios/qa/requirements.ts"
      provides: "4 validated QA Requirements scenarios"
      exports: ["qaRequirementsScenarios"]
    - path: "data/scenarios/qa/design.ts"
      provides: "4 validated QA Design scenarios"
      exports: ["qaDesignScenarios"]
  key_links:
    - from: "data/scenarios/qa/discovery.ts"
      to: "data/scenarios/schema.ts"
      via: "import validateScenario"
      pattern: "validateScenario"
---

<objective>
Author all 12 QA role SDLC-merged scenarios across 3 phases (Discovery, Requirements, Design), each covering all 4 friction dimensions with Indian B2B enterprise context.

Purpose: QA role content provides the quality/testing perspective, completing the 3-role first wave.
Output: 3 new scenario files in data/scenarios/qa/ with 4 validated ScenarioConfig objects each (12 total).
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-role-content-first-wave/03-CONTEXT.md
@.planning/phases/03-role-content-first-wave/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From data/scenarios/schema.ts:
```typescript
export function validateScenario(raw: unknown, filePath?: string): ScenarioConfig;

// Valid enum values:
// FrictionDimension: "client_friction" | "internal_bureaucracy" | "knowledge_gap" | "conflict_avoidance"
// SDLCPhase: "discovery" | "requirements" | "design" | "development" | "testing" | "launch" | "maintenance"
// Designation: "sales" | "pm" | "developer" | "qa" | "designer" | "ba" | "crm" | "strategy"
// Severity: "low" | "medium" | "high"
```

Template pattern (from data/scenarios/pm/discovery.ts):
```typescript
import { validateScenario } from "../schema";

const scenarioRaw = {
  id: "[role]-[phase]-[dimension]-01",
  role: "[role]",
  phase: "[phase]",
  frictionDimension: "[dimension]",
  title: "...",
  setup: "...",
  choices: [
    { id: "[role]-[phase-abbr]-c01-a", text: "...", frictionTag: "[dimension]", crossRoleImpact: [] },
    { id: "[role]-[phase-abbr]-c01-b", text: "...", frictionTag: "[dimension]", crossRoleImpact: [] },
  ],
  debrief: { whatHappened: "...", why: "...", how: "...", whoBecame: "...", tomorrowAction: "..." },
};

export const rolePhaseScenarios = [validateScenario(scenarioRaw, "data/scenarios/[role]/[phase].ts")];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 12 QA scenarios across 3 SDLC phases</name>
  <files>data/scenarios/qa/discovery.ts, data/scenarios/qa/requirements.ts, data/scenarios/qa/design.ts</files>
  <read_first>
    data/scenarios/pm/discovery.ts
    data/scenarios/schema.ts
    data/scenarios/types.ts
  </read_first>
  <action>
    First, create the `data/scenarios/qa/` directory if it does not exist.

    **qa/discovery.ts — CREATE with 4 scenarios:**
    - client_friction: "The Vague Acceptance Criteria" — Client's acceptance criteria says "system should be fast" with no measurable threshold. Setup: ₹4Cr IBC procurement platform, QA team needs to write test cases but acceptance criteria are subjective. tomorrowAction: QA entry criteria template with mandatory "measurable threshold" field for every acceptance criterion.
    - internal_bureaucracy: "The Test Plan Approval" — Test plan needs sign-off from 3 leads (dev lead, PM, security) before QA can start. One is on leave, another is in back-to-back meetings. Setup: SAP integration testing blocked by process. tomorrowAction: Test plan sign-off process with async approval and 48-hour auto-escalation.
    - knowledge_gap: "The Missing Use Cases" — Business analyst documented happy path only. No error flows, no edge cases, no concurrent user scenarios. Setup: TCS banking module with 15 transaction types, only 3 documented. tomorrowAction: Use case discovery questionnaire with mandatory "failure mode" and "concurrent access" sections.
    - conflict_avoidance: "The Inherited Scope" — QA asked to test a module they were never consulted on during requirements. Test scope is unrealistic for the timeline. Setup: IBC enterprise module, PM says "just test it by Friday." tomorrowAction: QA scope confirmation document with mandatory "scope vs timeline" assessment before test execution begins.

    Each scenario: role="qa", phase="discovery"
    IDs: qa-disc-client_friction-01, qa-disc-internal_bureaucracy-01, qa-disc-knowledge_gap-01, qa-disc-conflict_avoidance-01
    Export: `export const qaDiscoveryScenarios = [...]`

    **qa/requirements.ts — CREATE with 4 scenarios:**
    - client_friction: "The Moved Goalposts" — Client changes a core business rule after requirements are signed off. QA test cases are now invalid. Setup: ₹5Cr procurement system, pricing logic changed from tiered to flat rate mid-sprint. tomorrowAction: Requirements change impact checklist with "test case invalidation count" field.
    - internal_bureaucracy: "The Test Environment Queue" — Only one staging environment exists, shared across 4 teams. QA has a 3-day window but environment is occupied. Setup: SAP staging shared by dev, QA, UAT, and performance teams. tomorrowAction: Test environment booking process with conflict resolution SLA.
    - knowledge_gap: "The Undocumented Business Rule" — A critical pricing calculation has no documentation. The only person who knows the formula is a client-side BA who is hard to reach. Setup: IBC enterprise system, test case needs expected output but formula is tribal knowledge. tomorrowAction: Business rule register with mandatory "source" and "verification date" columns.
    - conflict_avoidance: "The Unchallenged Requirement" — QA spots a requirement that contradicts an existing feature but doesn't raise it because "PM already approved it." Setup: New auto-save feature conflicts with existing manual-save-only audit trail. tomorrowAction: Requirements review question bank with mandatory "conflict with existing" check.

    Each scenario: role="qa", phase="requirements"
    IDs: qa-req-client_friction-01, qa-req-internal_bureaucracy-01, qa-req-knowledge_gap-01, qa-req-conflict_avoidance-01
    Export: `export const qaRequirementsScenarios = [...]`

    **qa/design.ts — CREATE with 4 scenarios:**
    - client_friction: "The Accessibility Afterthought" — Design team delivers final mockups with zero accessibility annotations. Client contract requires WCAG 2.1 AA compliance. Setup: ₹3Cr government procurement portal, accessibility is contractual but design has no alt text, contrast ratios, or keyboard nav specs. tomorrowAction: Accessibility checklist with mandatory "contrast ratio", "keyboard navigation", and "screen reader" fields per component.
    - internal_bureaucracy: "The Design-QA Handover Gap" — Designers hand off Figma files directly to developers, bypassing QA. QA discovers discrepancies between design and implementation 2 sprints later. Setup: TCS enterprise dashboard, no QA involvement in design review. tomorrowAction: QA test case creation SLA starting from design handover (not dev complete).
    - knowledge_gap: "The Undocumented Edge State" — Design shows happy path only. Loading states, error states, empty states, and offline states are missing. Setup: IBC real-time dashboard with 8 widgets, each with 4 possible states — none designed. tomorrowAction: Edge state catalog template with mandatory entries for loading, error, empty, and offline per component.
    - conflict_avoidance: "The Silent Usability Issue" — QA noticed a confusing user flow during exploratory testing but didn't file it because "it matches the design." Setup: Checkout flow requires 7 clicks where competitors use 3, but the design was approved. tomorrowAction: Usability defect classification rubric with categories (critical UX, minor UX, cosmetic) and mandatory filing threshold.

    Each scenario: role="qa", phase="design"
    IDs: qa-des-client_friction-01, qa-des-internal_bureaucracy-01, qa-des-knowledge_gap-01, qa-des-conflict_avoidance-01
    Export: `export const qaDesignScenarios = [...]`

    CRITICAL: Every crossRoleImpact must use valid enum values. At least 1 scenario per file must have a non-empty crossRoleImpact array (e.g., QA decisions affecting developer or designer roles). All choice frictionTags must be valid FrictionDimension values. Indian B2B context throughout.
  </action>
  <verify>
    <automated>cd "/Users/shashankrai/Documents/Duplicate human-centric-lab-main 15 jan" && npx tsx -e "import { qaDiscoveryScenarios } from './data/scenarios/qa/discovery'; import { qaRequirementsScenarios } from './data/scenarios/qa/requirements'; import { qaDesignScenarios } from './data/scenarios/qa/design'; console.log('QA Discovery:', qaDiscoveryScenarios.length); console.log('QA Requirements:', qaRequirementsScenarios.length); console.log('QA Design:', qaDesignScenarios.length); const total = qaDiscoveryScenarios.length + qaRequirementsScenarios.length + qaDesignScenarios.length; if (total !== 12) { console.error('FAIL: Expected 12, got', total); process.exit(1); } console.log('ALL QA SCENARIOS VALID');"</automated>
  </verify>
  <acceptance_criteria>
    - data/scenarios/qa/ directory exists with 3 files
    - grep -c "validateScenario" data/scenarios/qa/discovery.ts returns 4
    - grep -c "validateScenario" data/scenarios/qa/requirements.ts returns 4
    - grep -c "validateScenario" data/scenarios/qa/design.ts returns 4
    - grep "tomorrowAction" data/scenarios/qa/requirements.ts returns 4 lines, none containing "try to" or "consider"
    - npx tsx import of all 3 files succeeds without Zod validation errors
  </acceptance_criteria>
  <done>12 QA scenarios across 3 phases, all pass Zod validation, each debrief tomorrowAction names a specific artifact, all 4 friction dimensions covered per phase</done>
</task>

<task type="auto">
  <name>Task 2: Validate QA scenario content quality</name>
  <files>data/scenarios/qa/discovery.ts, data/scenarios/qa/requirements.ts, data/scenarios/qa/design.ts</files>
  <read_first>
    data/scenarios/qa/discovery.ts
    data/scenarios/qa/requirements.ts
    data/scenarios/qa/design.ts
  </read_first>
  <action>
    Run comprehensive validation of all 12 QA scenarios:

    1. Import all 3 scenario files — if any throw, fix the Zod error in the source file
    2. Verify each file exports exactly 4 scenarios
    3. Verify all 4 friction dimensions are covered in each file (no duplicates, no gaps)
    4. Verify all 12 scenario IDs are unique and follow the qa-[phase]-[dimension]-01 pattern
    5. Verify no tomorrowAction contains generic phrases: "try to", "consider", "always", "be sure to", "communicate better"
    6. Verify at least 1 crossRoleImpact per phase file has a non-empty array
    7. Verify all role fields are "qa"

    Run validation inline with npx tsx. Fix any issues found.
  </action>
  <verify>
    <automated>cd "/Users/shashankrai/Documents/Duplicate human-centric-lab-main 15 jan" && npx tsx -e "
import { qaDiscoveryScenarios } from './data/scenarios/qa/discovery';
import { qaRequirementsScenarios } from './data/scenarios/qa/requirements';
import { qaDesignScenarios } from './data/scenarios/qa/design';

const all = [...qaDiscoveryScenarios, ...qaRequirementsScenarios, ...qaDesignScenarios];
const ids = all.map(s => s.id);
const uniqueIds = new Set(ids);
if (uniqueIds.size !== 12) { console.error('FAIL: duplicate IDs', ids); process.exit(1); }

const dims = ['client_friction','conflict_avoidance','internal_bureaucracy','knowledge_gap'];
for (const group of [qaDiscoveryScenarios, qaRequirementsScenarios, qaDesignScenarios]) {
  const groupDims = group.map(s => s.frictionDimension).sort();
  if (JSON.stringify(groupDims) !== JSON.stringify(dims)) {
    console.error('FAIL: missing dimension coverage', groupDims);
    process.exit(1);
  }
}

const allRoles = all.map(s => s.role);
if (allRoles.some(r => r !== 'qa')) { console.error('FAIL: wrong role', allRoles); process.exit(1); }

console.log('ALL VALIDATIONS PASSED');
"</automated>
  </verify>
  <acceptance_criteria>
    - Validation script exits with code 0
    - All 12 IDs are unique
    - All 4 friction dimensions present in each phase file
    - All scenarios have role: "qa"
  </acceptance_criteria>
  <done>All 12 QA scenarios validated: unique IDs, full friction dimension coverage per phase, correct role value, no generic tomorrowAction text</done>
</task>

</tasks>

<verification>
- All 3 QA scenario files import without Zod errors
- 12 total scenarios (4 per phase)
- All 4 friction dimensions covered in each phase
- Every tomorrowAction names a specific workflow artifact
- At least 1 crossRoleImpact per phase has a non-empty array
</verification>

<success_criteria>
- npx tsx import of all 3 QA scenario files succeeds
- 12 unique scenario IDs across all files
- Each phase covers client_friction, internal_bureaucracy, knowledge_gap, conflict_avoidance
- No generic tomorrowAction phrases
</success_criteria>

<output>
After completion, create `.planning/phases/03-role-content-first-wave/03-03-SUMMARY.md`
</output>
