---
phase: 03-role-content-first-wave
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/scenarios/developer/discovery.ts
  - data/scenarios/developer/requirements.ts
  - data/scenarios/developer/design.ts
autonomous: true
requirements: [ROLE-01, ROLE-02, ROLE-03, ROLE-04, ROLE-05, ROLE-06, BEHV-01]

must_haves:
  truths:
    - "Developer Discovery phase has 4 scenarios covering all 4 friction dimensions"
    - "Developer Requirements phase has 4 scenarios covering all 4 friction dimensions"
    - "Developer Design phase has 4 scenarios covering all 4 friction dimensions"
    - "Every Developer scenario debrief has a tomorrowAction naming a specific workflow artifact"
    - "All 12 Developer scenarios pass Zod validation at import time without errors"
  artifacts:
    - path: "data/scenarios/developer/discovery.ts"
      provides: "4 validated Developer Discovery scenarios"
      exports: ["developerDiscoveryScenarios"]
    - path: "data/scenarios/developer/requirements.ts"
      provides: "4 validated Developer Requirements scenarios"
      exports: ["developerRequirementsScenarios"]
    - path: "data/scenarios/developer/design.ts"
      provides: "4 validated Developer Design scenarios"
      exports: ["developerDesignScenarios"]
  key_links:
    - from: "data/scenarios/developer/discovery.ts"
      to: "data/scenarios/schema.ts"
      via: "import validateScenario"
      pattern: "validateScenario"
---

<objective>
Author all 12 Developer role SDLC-merged scenarios across 3 phases (Discovery, Requirements, Design), each covering all 4 friction dimensions with Indian B2B enterprise context.

Purpose: Developer role content provides a technical perspective complementing PM's project management view.
Output: 3 new scenario files in data/scenarios/developer/ with 4 validated ScenarioConfig objects each (12 total).
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-role-content-first-wave/03-CONTEXT.md
@.planning/phases/03-role-content-first-wave/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From data/scenarios/schema.ts:
```typescript
export function validateScenario(raw: unknown, filePath?: string): ScenarioConfig;

// Valid enum values:
// FrictionDimension: "client_friction" | "internal_bureaucracy" | "knowledge_gap" | "conflict_avoidance"
// SDLCPhase: "discovery" | "requirements" | "design" | "development" | "testing" | "launch" | "maintenance"
// Designation: "sales" | "pm" | "developer" | "qa" | "designer" | "ba" | "crm" | "strategy"
// Severity: "low" | "medium" | "high"
```

From data/scenarios/pm/discovery.ts (TEMPLATE pattern to follow):
```typescript
import { validateScenario } from "../schema";

const clientFriction = {
  id: "pm-discovery-client-01",
  role: "pm",
  phase: "discovery",
  frictionDimension: "client_friction",
  title: "The Undocumented Assumption",
  setup: "A ₹3Cr IBC client wants an AI dashboard...",
  choices: [
    { id: "pm-disc-c01-a", text: "...", frictionTag: "conflict_avoidance", crossRoleImpact: [{
      affectedRole: "ba", affectedPhase: "requirements", description: "...", severity: "high"
    }] },
    { id: "pm-disc-c01-b", text: "...", frictionTag: "client_friction", crossRoleImpact: [] },
  ],
  debrief: {
    whatHappened: "...", why: "...", how: "...", whoBecame: "...",
    tomorrowAction: "In your next discovery call close-out, add one mandatory line to your summary doc...",
  },
};

export const pmDiscoveryScenarios = [validateScenario(clientFriction, "data/scenarios/pm/discovery.ts"), ...];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 12 Developer scenarios across 3 SDLC phases</name>
  <files>data/scenarios/developer/discovery.ts, data/scenarios/developer/requirements.ts, data/scenarios/developer/design.ts</files>
  <read_first>
    data/scenarios/pm/discovery.ts
    data/scenarios/schema.ts
    data/scenarios/types.ts
  </read_first>
  <action>
    First, create the `data/scenarios/developer/` directory if it does not exist.

    **developer/discovery.ts — CREATE with 4 scenarios:**
    - client_friction: "The Undefined API Contract" — Client's procurement system has no documented API. Developer must reverse-engineer integration from a PDF user manual. Setup: ₹3Cr TCS integration project, client IT team says "just use the portal." tomorrowAction: API contract template with mandatory fields (auth, rate limits, error codes, versioning).
    - internal_bureaucracy: "The Architecture Sign-Off" — New microservice needs architecture review board approval, board meets monthly. Sprint starts in 5 days. Setup: SAP migration, team needs to add a new service but ARB calendar is full. tomorrowAction: Architecture Decision Record (ADR) template for async review.
    - knowledge_gap: "The Undocumented Legacy" — Legacy codebase has no documentation. The original developer left 2 years ago. Setup: IBC enterprise system with 200K lines, no README, no architecture diagram. tomorrowAction: Legacy system dependency map template (entry points, data flows, external calls).
    - conflict_avoidance: "The Optimistic Estimate" — Tech lead gives optimistic estimate to avoid pushback from PM. Setup: Feature requires SAP BAPI integration, estimate says 3 sprints but real complexity is 5. tomorrowAction: Estimation checklist with mandatory "complexity multiplier" for external integrations.

    Each scenario: role="developer", phase="discovery"
    IDs: dev-disc-client_friction-01, dev-disc-internal_bureaucracy-01, dev-disc-knowledge_gap-01, dev-disc-conflict_avoidance-01
    Export: `export const developerDiscoveryScenarios = [...]`

    **developer/requirements.ts — CREATE with 4 scenarios:**
    - client_friction: "The Real-Time Trap" — Client demands real-time sync but existing infrastructure is batch-only. Setup: ₹5Cr procurement portal, client wants live inventory but backend is SAP batch job. tomorrowAction: Non-functional requirements checklist with "sync mechanism" mandatory field.
    - internal_bureaucracy: "The Sprint Entry Gate" — Story cannot enter sprint without 3 approvals (QA sign-off, security review, accessibility audit). Setup: Critical bug fix blocked by process. tomorrowAction: Definition-of-ready checklist with emergency bypass criteria.
    - knowledge_gap: "The Missing Context" — Requirements reference a business rule nobody on the dev team understands. Setup: IBC compliance rule that triggers different pricing tiers, no documentation. tomorrowAction: Requirements clarification questions template with "business rule source" mandatory field.
    - conflict_avoidance: "The Absorbed Ambiguity" — Developer spots ambiguous acceptance criteria but doesn't raise it to avoid rework. Setup: Acceptance criteria says "handle edge cases" — no definition of which ones. tomorrowAction: Acceptance criteria review checklist with "edge case enumeration" gate.

    Each scenario: role="developer", phase="requirements"
    IDs: dev-req-client_friction-01, dev-req-internal_bureaucracy-01, dev-req-knowledge_gap-01, dev-req-conflict_avoidance-01
    Export: `export const developerRequirementsScenarios = [...]`

    **developer/design.ts — CREATE with 4 scenarios:**
    - client_friction: "The Late Integration Request" — Client adds a third-party payment gateway requirement after design is 80% complete. Setup: ₹4Cr e-commerce platform, client's finance team mandates a specific payment provider. tomorrowAction: Design-to-dev handover template with "external integration" section.
    - internal_bureaucracy: "The Design Freeze Delay" — Design freeze keeps getting pushed because stakeholders add "one more thing." Setup: Enterprise portal redesign, 3rd design freeze extension in 2 months. tomorrowAction: Design review timeline document with freeze date and no-change criteria.
    - knowledge_gap: "The Undocumented Design System" — Design system exists but no one documented the token values, spacing rules, or component variants. Setup: TCS design system with 40+ components, no Storybook, no docs. tomorrowAction: Design token documentation template (colors, spacing, typography, component states).
    - conflict_avoidance: "The Unchallenged Spec" — Developer sees a spec that will cause performance issues but doesn't push back on the designer. Setup: Designer spec calls for 60fps animation on a data-heavy dashboard with 500 rows. tomorrowAction: Dev-QA pre-implementation checklist with "performance concern" flag.

    Each scenario: role="developer", phase="design"
    IDs: dev-des-client_friction-01, dev-des-internal_bureaucracy-01, dev-des-knowledge_gap-01, dev-des-conflict_avoidance-01
    Export: `export const developerDesignScenarios = [...]`

    CRITICAL: Every crossRoleImpact must use valid enum values. At least 1 scenario per file must have a non-empty crossRoleImpact array. All choice frictionTags must be valid FrictionDimension values. Indian B2B context throughout (IBC, SAP, TCS, enterprise procurement).
  </action>
  <verify>
    <automated>cd "/Users/shashankrai/Documents/Duplicate human-centric-lab-main 15 jan" && npx tsx -e "import { developerDiscoveryScenarios } from './data/scenarios/developer/discovery'; import { developerRequirementsScenarios } from './data/scenarios/developer/requirements'; import { developerDesignScenarios } from './data/scenarios/developer/design'; console.log('Dev Discovery:', developerDiscoveryScenarios.length); console.log('Dev Requirements:', developerRequirementsScenarios.length); console.log('Dev Design:', developerDesignScenarios.length); const total = developerDiscoveryScenarios.length + developerRequirementsScenarios.length + developerDesignScenarios.length; if (total !== 12) { console.error('FAIL: Expected 12, got', total); process.exit(1); } console.log('ALL DEVELOPER SCENARIOS VALID');"</automated>
  </verify>
  <acceptance_criteria>
    - data/scenarios/developer/ directory exists with 3 files
    - grep -c "validateScenario" data/scenarios/developer/discovery.ts returns 4
    - grep -c "validateScenario" data/scenarios/developer/requirements.ts returns 4
    - grep -c "validateScenario" data/scenarios/developer/design.ts returns 4
    - grep "tomorrowAction" data/scenarios/developer/requirements.ts returns 4 lines, none containing "try to" or "consider"
    - npx tsx import of all 3 files succeeds without Zod validation errors
  </acceptance_criteria>
  <done>12 Developer scenarios across 3 phases, all pass Zod validation, each debrief tomorrowAction names a specific artifact, all 4 friction dimensions covered per phase</done>
</task>

<task type="auto">
  <name>Task 2: Validate Developer scenario content quality</name>
  <files>data/scenarios/developer/discovery.ts, data/scenarios/developer/requirements.ts, data/scenarios/developer/design.ts</files>
  <read_first>
    data/scenarios/developer/discovery.ts
    data/scenarios/developer/requirements.ts
    data/scenarios/developer/design.ts
  </read_first>
  <action>
    Run comprehensive validation of all 12 Developer scenarios:

    1. Import all 3 scenario files — if any throw, fix the Zod error in the source file
    2. Verify each file exports exactly 4 scenarios
    3. Verify all 4 friction dimensions are covered in each file (no duplicates, no gaps)
    4. Verify all 12 scenario IDs are unique and follow the dev-[phase]-[dimension]-01 pattern
    5. Verify no tomorrowAction contains generic phrases: "try to", "consider", "always", "be sure to", "communicate better"
    6. Verify at least 1 crossRoleImpact per phase file has a non-empty array
    7. Verify all role fields are "developer" (not "dev")

    Run validation inline with npx tsx. Fix any issues found.
  </action>
  <verify>
    <automated>cd "/Users/shashankrai/Documents/Duplicate human-centric-lab-main 15 jan" && npx tsx -e "
import { developerDiscoveryScenarios } from './data/scenarios/developer/discovery';
import { developerRequirementsScenarios } from './data/scenarios/developer/requirements';
import { developerDesignScenarios } from './data/scenarios/developer/design';

const all = [...developerDiscoveryScenarios, ...developerRequirementsScenarios, ...developerDesignScenarios];
const ids = all.map(s => s.id);
const uniqueIds = new Set(ids);
if (uniqueIds.size !== 12) { console.error('FAIL: duplicate IDs', ids); process.exit(1); }

const dims = ['client_friction','conflict_avoidance','internal_bureaucracy','knowledge_gap'];
for (const group of [developerDiscoveryScenarios, developerRequirementsScenarios, developerDesignScenarios]) {
  const groupDims = group.map(s => s.frictionDimension).sort();
  if (JSON.stringify(groupDims) !== JSON.stringify(dims)) {
    console.error('FAIL: missing dimension coverage', groupDims);
    process.exit(1);
  }
}

const allRoles = all.map(s => s.role);
if (allRoles.some(r => r !== 'developer')) { console.error('FAIL: wrong role', allRoles); process.exit(1); }

console.log('ALL VALIDATIONS PASSED');
"</automated>
  </verify>
  <acceptance_criteria>
    - Validation script exits with code 0
    - All 12 IDs are unique
    - All 4 friction dimensions present in each phase file
    - All scenarios have role: "developer"
  </acceptance_criteria>
  <done>All 12 Developer scenarios validated: unique IDs, full friction dimension coverage per phase, correct role value, no generic tomorrowAction text</done>
</task>

</tasks>

<verification>
- All 3 Developer scenario files import without Zod errors
- 12 total scenarios (4 per phase)
- All 4 friction dimensions covered in each phase
- Every tomorrowAction names a specific workflow artifact
- At least 1 crossRoleImpact per phase has a non-empty array
</verification>

<success_criteria>
- npx tsx import of all 3 Developer scenario files succeeds
- 12 unique scenario IDs across all files
- Each phase covers client_friction, internal_bureaucracy, knowledge_gap, conflict_avoidance
- No generic tomorrowAction phrases
</success_criteria>

<output>
After completion, create `.planning/phases/03-role-content-first-wave/03-02-SUMMARY.md`
</output>
