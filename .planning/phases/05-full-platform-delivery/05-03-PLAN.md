---
phase: 05-full-platform-delivery
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - data/scenarios/strategy/discovery.ts
  - data/scenarios/strategy/requirements.ts
  - data/scenarios/strategy/design.ts
autonomous: true
requirements: [AUDT-01]

must_haves:
  truths:
    - "Strategy role has 12 scenarios covering Discovery, Requirements, Design phases with all 4 friction dimensions"
    - "All scenarios pass Zod validation at import time without errors"
    - "Every tomorrowAction names a specific workflow artifact, not generic advice"
  artifacts:
    - path: "data/scenarios/strategy/discovery.ts"
      provides: "4 Strategy Discovery scenarios"
      exports: ["strategyDiscoveryScenarios"]
    - path: "data/scenarios/strategy/requirements.ts"
      provides: "4 Strategy Requirements scenarios"
      exports: ["strategyRequirementsScenarios"]
    - path: "data/scenarios/strategy/design.ts"
      provides: "4 Strategy Design scenarios"
      exports: ["strategyDesignScenarios"]
  key_links:
    - from: "data/scenarios/strategy/*.ts"
      to: "data/scenarios/schema.ts"
      via: "validateScenario import"
      pattern: "import.*validateScenario.*from.*schema"
---

<objective>
Author 12 SDLC-merged scenarios for the Strategy role (3 phases x 4 friction dimensions).

Purpose: Strategy is the final remaining role. Scenarios focus on business outcomes, market positioning, signal vs noise, and investment sequencing in enterprise software delivery.

Output: 3 scenario data files with Zod-validated ScenarioConfig objects, Indian B2B context, crossRoleImpact arrays, and artifact-specific tomorrowAction fields.
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-full-platform-delivery/05-CONTEXT.md
@.planning/phases/05-full-platform-delivery/05-RESEARCH.md

<interfaces>
From data/scenarios/pm/discovery.ts (reference pattern):
```typescript
import { validateScenario } from "../schema";

const rawClientFriction = { id, role, phase, frictionDimension, title, setup, choices: [{id, text, frictionTag, crossRoleImpact: [{affectedRole, affectedPhase, description, severity}]}, {id, text, frictionTag, crossRoleImpact: []}], debrief: {whatHappened, why, how, whoBecame, tomorrowAction} };

export const [role][Phase]Scenarios = [validateScenario(raw1, path), validateScenario(raw2, path), validateScenario(raw3, path), validateScenario(raw4, path)];
```

From data/scenarios/types.ts:
```typescript
type Designation = "sales" | "crm" | "pm" | "developer" | "qa" | "designer" | "strategy" | "ba";
type FrictionDimension = "client_friction" | "internal_bureaucracy" | "knowledge_gap" | "conflict_avoidance";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Strategy role Discovery and Requirements scenarios (2 files, 8 scenarios)</name>
  <files>data/scenarios/strategy/discovery.ts, data/scenarios/strategy/requirements.ts</files>
  <read_first>
    - data/scenarios/pm/discovery.ts (reference file for structure and quality bar)
    - data/scenarios/pm/requirements.ts (reference for requirements phase)
    - data/scenarios/schema.ts (Zod schema)
    - data/scenarios/types.ts (type definitions)
  </read_first>
  <action>
Create directory `data/scenarios/strategy/` and 2 scenario files.

**Strategy Discovery (data/scenarios/strategy/discovery.ts):**
Export `strategyDiscoveryScenarios` array with 4 validated scenarios:
- `strategy-discovery-client_friction-01` — "The Buzzword Board" — Board wants "AI and mobile" without user research. ₹8Cr product direction decision. Strategy must present to executives who want trend alignment, not evidence. Friction choice: build the strategy deck around AI/mobile trend reports without user data. Better: present a 2-track approach: quick market signal analysis AND a 4-week user research sprint before committing direction. crossRoleImpact: [{affectedRole: "pm", affectedPhase: "discovery", severity: "high", description: "PM begins discovery for a product direction chosen by trend, not evidence — pivots become expensive when user data contradicts board assumptions"}]. tomorrowAction: "Add a 'Evidence Gate' slide to your next strategy presentation: 'This direction is based on [market trend / user research / competitor analysis]. Confidence level: [high/medium/low]. Validation plan: [specific next step].'"
- `strategy-discovery-internal_bureaucracy-01` — "The Committee Quarterly" — Strategic initiative requires 3 committee approvals, each meets quarterly. ₹5Cr investment decision. Friction choice: wait for all 3 quarterly committee cycles (9+ months). Better: request an extraordinary joint session with a decision deadline tied to market window. crossRoleImpact: [{affectedRole: "ba", affectedPhase: "discovery", severity: "high", description: "BA cannot begin discovery until strategic direction is approved — 9-month delay renders original market research obsolete"}]. tomorrowAction: "For initiatives with quarterly approval gates, draft a 'Joint Committee Expedite Request' with: market window deadline, cost of 1-quarter delay (₹X revenue risk), and proposed single-session agenda."
- `strategy-discovery-knowledge_gap-01` — "The Stale Data" — Strategy depends on a market report that is 2 years old. Competitive landscape has shifted. ₹4Cr product bet. Friction choice: present the 2-year-old data as current in the strategy document. Better: commission a quick competitive refresh (2 weeks) before finalizing the strategy. crossRoleImpact: [{affectedRole: "sales", affectedPhase: "discovery", severity: "medium", description: "Sales positions the product using outdated competitive differentiators — loses deals where competitors have already closed the gap"}]. tomorrowAction: "Add a 'Data Freshness Audit' to every strategy document: for each data source, list publication date, relevance confidence (high/medium/low), and refresh action if older than 12 months."
- `strategy-discovery-conflict_avoidance-01` — "The Dual Priority" — Two senior stakeholders have contradictory strategic priorities. ₹6Cr portfolio decision. Strategy team tries to satisfy both without resolving the conflict. Friction choice: present a plan that ambiguously accommodates both priorities. Better: present both priorities explicitly with trade-offs and request a single direction decision. crossRoleImpact: [{affectedRole: "pm", affectedPhase: "requirements", severity: "high", description: "PM receives a strategy doc with two conflicting priorities — requirements work starts on both tracks, doubling effort until one is killed"}]. tomorrowAction: "When two stakeholders have conflicting priorities, create a 'Strategic Trade-Off Brief' with: Priority A (benefits, costs, risks), Priority B (benefits, costs, risks), and 'We cannot do both because [specific resource/timeline constraint].'"

**Strategy Requirements (data/scenarios/strategy/requirements.ts):**
Export `strategyRequirementsScenarios` array with 4 validated scenarios:
- `strategy-req-client_friction-01` — "The Moving Target" — Client's strategic objectives shift between meetings. ₹3Cr engagement. Strategy team is building against a moving target. Friction choice: update the strategy document after each meeting without flagging the pattern. Better: present the change log to the client showing 4 direction changes in 6 weeks and request a strategy freeze. crossRoleImpact: [{affectedRole: "ba", affectedPhase: "requirements", severity: "high", description: "BA restarts requirements gathering with each strategic pivot — 3 weeks of requirements work discarded per change"}]. tomorrowAction: "Maintain a 'Strategic Direction Change Log' for every engagement: date, previous direction, new direction, reason, impact on in-progress work. Present at any meeting where direction changes for the 3rd time."
- `strategy-req-internal_bureaucracy-01` — "The Veto Power" — Strategy document must pass political review by all department heads, each with veto power. ₹5Cr initiative. Friction choice: water down the strategy to eliminate anything any department might veto. Better: present the full strategy with department-specific impact assessments and negotiate trade-offs explicitly. crossRoleImpact: [{affectedRole: "pm", affectedPhase: "requirements", severity: "medium", description: "PM receives a diluted strategy that doesn't commit to measurable outcomes — requirements become a wish list without priority"}]. tomorrowAction: "Before circulating a strategy for review, create a 'Department Impact Matrix' showing: each recommendation's impact on each department (positive/neutral/negative), with pre-negotiated mitigation for negative impacts."
- `strategy-req-knowledge_gap-01` — "The Unvalidated Assumption" — Strategy relies on user behavior assumptions never validated. Executives want to skip discovery. ₹4Cr product direction. Friction choice: proceed with the assumption and plan discovery "later." Better: propose a 2-week rapid validation sprint before committing engineering resources. crossRoleImpact: [{affectedRole: "developer", affectedPhase: "development", severity: "high", description: "Developer builds features based on unvalidated assumptions — product usage data after launch contradicts core premise, requiring architectural rework"}]. tomorrowAction: "For every strategic assumption, add a 'Validation Status' tag: Validated (with evidence link), Assumed (with validation plan), or Unknown (with risk assessment). No assumption without a tag."
- `strategy-req-conflict_avoidance-01` — "The Omitted Finding" — Strategy identifies that proposed direction conflicts with a promised client commitment. ₹3Cr client, ₹5Cr new direction. Strategy team omits the finding from the report. Friction choice: omit the conflict from the strategy report to avoid political fallout. Better: include the conflict with a recommended resolution path. crossRoleImpact: [{affectedRole: "crm", affectedPhase: "requirements", severity: "high", description: "CRM discovers the conflict when the client raises it — relationship damage from broken promise has no recovery plan"}]. tomorrowAction: "Add a 'Commitment Conflict Check' to every strategy document: list all existing client commitments that the proposed direction affects, with resolution plan for each."
  </action>
  <verify>
    <automated>cd "/Users/shashankrai/Documents/Duplicate human-centric-lab-main 15 jan" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - data/scenarios/strategy/discovery.ts contains `export const strategyDiscoveryScenarios`
    - data/scenarios/strategy/requirements.ts contains `export const strategyRequirementsScenarios`
    - Each file contains exactly 4 `validateScenario(` calls
    - All scenario IDs start with `strategy-`
    - Every scenario has `role: "strategy"`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Strategy Discovery and Requirements phases have 8 Zod-validated scenarios with Indian B2B context.</done>
</task>

<task type="auto">
  <name>Task 2: Author Strategy role Design scenarios (1 file, 4 scenarios)</name>
  <files>data/scenarios/strategy/design.ts</files>
  <read_first>
    - data/scenarios/pm/design.ts (reference file for design phase pattern)
    - data/scenarios/schema.ts (Zod schema)
    - data/scenarios/types.ts (type definitions)
    - data/scenarios/strategy/discovery.ts (just created — verify consistency)
  </read_first>
  <action>
Create `data/scenarios/strategy/design.ts`.

**Strategy Design (data/scenarios/strategy/design.ts):**
Export `strategyDesignScenarios` array with 4 validated scenarios:
- `strategy-des-client_friction-01` — "The Direction Collision" — Strategy-level UX direction conflicts with what dev team has already built. ₹4Cr product, 3 months of development done. Friction choice: adapt the strategy to match what's already built (sunk cost reasoning). Better: assess the rework cost vs. opportunity cost of wrong direction, present trade-offs to leadership. crossRoleImpact: [{affectedRole: "developer", affectedPhase: "development", severity: "high", description: "Developer faces rework on 3 months of completed work — team morale drops, velocity slows for the next quarter"}]. tomorrowAction: "Before approving a strategy pivot that affects in-progress work, complete a 'Pivot Cost Assessment' showing: sunk cost, rework cost, opportunity cost of NOT pivoting, and recommended path with timeline."
- `strategy-des-internal_bureaucracy-01` — "The Instrumentation Gap" — Strategic KPI dashboard requires buy-in from 7 teams. 3 declined due to resource constraints. Friction choice: launch the dashboard with partial data and call it "Phase 1." Better: escalate the 3 declining teams to their VPs with a business impact quantification. crossRoleImpact: [{affectedRole: "pm", affectedPhase: "design", severity: "medium", description: "PM designs product metrics against a dashboard missing 40% of the data sources — decisions are made on incomplete signals"}]. tomorrowAction: "For any cross-team instrumentation initiative, create a 'Team Buy-In Tracker' with: team name, data source needed, effort estimate, current status (committed/declining/negotiating), and escalation path for declining teams."
- `strategy-des-knowledge_gap-01` — "The Measurement Gap" — Strategy can't connect user adoption to business outcomes because data infrastructure wasn't built for it. ₹6Cr strategic bet. Friction choice: present adoption numbers as proxy for business impact. Better: commission a measurement infrastructure plan before claiming strategic success. crossRoleImpact: [{affectedRole: "crm", affectedPhase: "design", severity: "medium", description: "CRM presents unverifiable ROI claims to clients during renewals — credibility erodes when clients ask for proof"}]. tomorrowAction: "For every strategic initiative, define a 'Measurement Architecture' document on day 1: what metrics prove success, what data sources exist today, what instrumentation must be built, and by when."
- `strategy-des-conflict_avoidance-01` — "The Delayed Reckoning" — Post-launch data shows strategy had a flaw. Strategy team delays presenting findings. ₹5Cr decision. Friction choice: delay the presentation until you can "spin" the data more favorably. Better: present findings within 2 weeks with a correction plan and lessons learned. crossRoleImpact: [{affectedRole: "pm", affectedPhase: "requirements", severity: "high", description: "PM continues building on a flawed strategic premise for weeks — the longer the delay, the more work is wasted"}]. tomorrowAction: "Establish a 'Strategic Review Cadence' rule: post-launch data review at week 2 and week 8. If data contradicts the hypothesis, present findings within 5 business days with a recommended pivot or persist decision."

All scenarios: Indian B2B context, debrief fields under 150 words, scenario IDs prefixed with `strategy-`.
  </action>
  <verify>
    <automated>cd "/Users/shashankrai/Documents/Duplicate human-centric-lab-main 15 jan" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - data/scenarios/strategy/design.ts contains `export const strategyDesignScenarios`
    - File contains exactly 4 `validateScenario(` calls
    - All scenario IDs start with `strategy-`
    - Every scenario has `role: "strategy"`
    - Every debrief has all 5 fields
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Strategy role has all 12 scenarios across 3 SDLC phases complete.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- All 3 files export named arrays of exactly 4 ScenarioConfig objects
- `npm run build` succeeds
</verification>

<success_criteria>
12 Strategy scenarios created, all Zod-validated, all with Indian B2B context, crossRoleImpact arrays, and artifact-specific tomorrowActions under 150 words.
</success_criteria>

<output>
After completion, create `.planning/phases/05-full-platform-delivery/05-03-SUMMARY.md`
</output>
