---
phase: 05-full-platform-delivery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/scenarios/ba/discovery.ts
  - data/scenarios/ba/requirements.ts
  - data/scenarios/ba/design.ts
  - data/scenarios/crm/discovery.ts
  - data/scenarios/crm/requirements.ts
  - data/scenarios/crm/design.ts
autonomous: true
requirements: [AUDT-01]

must_haves:
  truths:
    - "BA role has 12 scenarios covering Discovery, Requirements, Design phases with all 4 friction dimensions"
    - "CRM role has 12 scenarios covering Discovery, Requirements, Design phases with all 4 friction dimensions"
    - "All scenarios pass Zod validation at import time without errors"
    - "Every tomorrowAction names a specific workflow artifact, not generic advice"
  artifacts:
    - path: "data/scenarios/ba/discovery.ts"
      provides: "4 BA Discovery scenarios"
      exports: ["baDiscoveryScenarios"]
    - path: "data/scenarios/ba/requirements.ts"
      provides: "4 BA Requirements scenarios"
      exports: ["baRequirementsScenarios"]
    - path: "data/scenarios/ba/design.ts"
      provides: "4 BA Design scenarios"
      exports: ["baDesignScenarios"]
    - path: "data/scenarios/crm/discovery.ts"
      provides: "4 CRM Discovery scenarios"
      exports: ["crmDiscoveryScenarios"]
    - path: "data/scenarios/crm/requirements.ts"
      provides: "4 CRM Requirements scenarios"
      exports: ["crmRequirementsScenarios"]
    - path: "data/scenarios/crm/design.ts"
      provides: "4 CRM Design scenarios"
      exports: ["crmDesignScenarios"]
  key_links:
    - from: "data/scenarios/ba/*.ts"
      to: "data/scenarios/schema.ts"
      via: "validateScenario import"
      pattern: "import.*validateScenario.*from.*schema"
    - from: "data/scenarios/crm/*.ts"
      to: "data/scenarios/schema.ts"
      via: "validateScenario import"
      pattern: "import.*validateScenario.*from.*schema"
---

<objective>
Author 24 SDLC-merged scenarios for BA and CRM roles (12 each: 3 phases x 4 friction dimensions).

Purpose: BA and CRM are 2 of the 5 remaining roles needing full SDLC journeys. BA scenarios focus on requirement clarity, stakeholder alignment, and UAT facilitation. CRM scenarios focus on customer retention, escalation handling, and renewal friction.

Output: 6 scenario data files (3 per role) with Zod-validated ScenarioConfig objects, Indian B2B context, crossRoleImpact arrays, and artifact-specific tomorrowAction fields.
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-full-platform-delivery/05-CONTEXT.md
@.planning/phases/05-full-platform-delivery/05-RESEARCH.md

<interfaces>
From data/scenarios/pm/discovery.ts (reference pattern):
```typescript
import { validateScenario } from "../schema";

const rawClientFriction = {
  id: "[role]-[phase]-[dimension]-01",
  role: "[role]",
  phase: "[phase]",
  frictionDimension: "client_friction",
  title: "Descriptive title",
  setup: "₹ amount + named client (IBC/SAP/TCS) + specific situation + time pressure",
  choices: [
    { id: "[role]-[phase-short]-cf01-a", text: "Friction choice", frictionTag: "client_friction", crossRoleImpact: [{affectedRole, affectedPhase, description, severity}] },
    { id: "[role]-[phase-short]-cf01-b", text: "Better choice", frictionTag: "client_friction", crossRoleImpact: [] },
  ],
  debrief: { whatHappened, why, how, whoBecame, tomorrowAction },
};

export const [role][Phase]Scenarios = [
  validateScenario(rawClientFriction, "data/scenarios/[role]/[phase].ts"),
  validateScenario(rawInternalBureaucracy, "data/scenarios/[role]/[phase].ts"),
  validateScenario(rawKnowledgeGap, "data/scenarios/[role]/[phase].ts"),
  validateScenario(rawConflictAvoidance, "data/scenarios/[role]/[phase].ts"),
];
```

From data/scenarios/types.ts:
```typescript
type Designation = "sales" | "crm" | "pm" | "developer" | "qa" | "designer" | "strategy" | "ba";
type SDLCPhase = "discovery" | "requirements" | "design" | "development" | "testing" | "launch" | "maintenance";
type FrictionDimension = "client_friction" | "internal_bureaucracy" | "knowledge_gap" | "conflict_avoidance";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author BA role SDLC scenarios (3 files, 12 scenarios)</name>
  <files>data/scenarios/ba/discovery.ts, data/scenarios/ba/requirements.ts, data/scenarios/ba/design.ts</files>
  <read_first>
    - data/scenarios/pm/discovery.ts (reference file for structure and quality bar)
    - data/scenarios/pm/requirements.ts (reference for requirements phase)
    - data/scenarios/schema.ts (Zod schema)
    - data/scenarios/types.ts (type definitions)
  </read_first>
  <action>
Create directory `data/scenarios/ba/` and 3 scenario files.

**BA Discovery (data/scenarios/ba/discovery.ts):**
Export `baDiscoveryScenarios` array with 4 validated scenarios:
- `ba-discovery-client_friction-01` — "The Unstructured Kickoff" — 40-page kickoff notes, no priority order. ₹3.5Cr IBC project. BA must structure requirements without a North Star. Friction choice: start documenting requirements from raw notes without stakeholder priority alignment. Better: schedule a 1-hour priority mapping session with the top 3 stakeholders before writing any requirements. crossRoleImpact: [{affectedRole: "pm", affectedPhase: "requirements", severity: "high", description: "PM inherits requirements with no priority hierarchy — sprint planning becomes a negotiation without data"}]. tomorrowAction: "Add a 'Priority Alignment Workshop' to your kickoff template — 1 hour, top 3 stakeholders, output: ranked list of 5 non-negotiable requirements and 10 flexible ones."
- `ba-discovery-internal_bureaucracy-01` — "The Access Maze" — BA needs legacy system documentation to write requirements. Access requires IT approvals across 3 departments. ₹4Cr SAP migration. Friction choice: write requirements based on available documentation only. Better: escalate access request with business impact quantification to fast-track approvals. crossRoleImpact: [{affectedRole: "developer", affectedPhase: "development", severity: "high", description: "Developer builds against incomplete requirements — integration points with legacy system surface as blockers during implementation"}]. tomorrowAction: "Create a 'System Access Request Tracker' spreadsheet filed on day 1 of discovery, listing every system needed, approval owner, and deadline — escalate any access not granted within 5 business days."
- `ba-discovery-knowledge_gap-01` — "The Departing Expert" — Subject matter expert retiring in 6 weeks, won't be available for UAT. ₹2Cr project for a financial compliance module. Friction choice: proceed with discovery assuming documentation is sufficient. Better: schedule intensive knowledge transfer sessions before the expert leaves. crossRoleImpact: [{affectedRole: "qa", affectedPhase: "testing", severity: "high", description: "QA has no domain expert for UAT validation — acceptance criteria cannot be verified against business intent"}]. tomorrowAction: "When a key SME has a departure date, create a 'Knowledge Transfer Schedule' within 48 hours covering: 3 structured interviews, 1 walkthrough of critical workflows, and a recorded Q&A session."
- `ba-discovery-conflict_avoidance-01` — "The Contradictory Stakeholders" — IT head and business head have contradictory requirements. ₹5Cr enterprise project. BA documents both without flagging the conflict. Friction choice: include both contradictory requirements in the discovery report as-is. Better: flag the conflict explicitly in the discovery report with both positions and request stakeholder alignment before requirements. crossRoleImpact: [{affectedRole: "designer", affectedPhase: "design", severity: "high", description: "Designer receives contradictory requirements — designs against one stakeholder's vision, the other rejects it during review"}]. tomorrowAction: "Add a 'Conflict Register' section to your discovery report template — list every requirement where stakeholders disagree, with both positions documented and a mandatory resolution meeting scheduled."

**BA Requirements (data/scenarios/ba/requirements.ts):**
Export `baRequirementsScenarios` array with 4 validated scenarios:
- `ba-req-client_friction-01` — "The Last-Minute Pivot" — Client changes acceptance criteria 2 days before requirements sign-off. ₹3Cr project. Friction choice: absorb the changes to avoid delaying sign-off. Better: document the change impact (timeline, budget, scope) and present trade-offs before accepting. crossRoleImpact: [{affectedRole: "pm", affectedPhase: "requirements", severity: "high"}]. tomorrowAction: "Create a 'Change Impact Assessment' one-pager template: changed requirement, affected modules, timeline impact (+X days), budget impact (+₹Y), alternative approaches."
- `ba-req-internal_bureaucracy-01` — "The Wet Signature" — Sign-off requires physical documents with wet signatures from 5 executives in different cities. Friction choice: wait for all physical signatures sequentially. Better: propose digital sign-off with audit trail, escalate to project sponsor. crossRoleImpact: [{affectedRole: "developer", affectedPhase: "development", severity: "medium"}]. tomorrowAction: "Draft a 'Digital Sign-Off Proposal' for your project sponsor with: audit trail evidence, legal precedent for digital signatures in enterprise contracts, projected time savings."
- `ba-req-knowledge_gap-01` — "The Regulatory Blind Spot" — BA writes requirements for financial calculation without regulatory context. Technically valid but legally non-compliant spec. Friction choice: submit the spec assuming developers will handle compliance. Better: consult with compliance team before finalizing financial requirements. crossRoleImpact: [{affectedRole: "developer", affectedPhase: "development", severity: "high"}]. tomorrowAction: "Add a 'Regulatory Compliance Checklist' to your requirements template for any module involving financial calculations, data privacy, or user consent."
- `ba-req-conflict_avoidance-01` — "The Vague Revision" — Developer says requirements are "too vague to build." BA revises without involving the client who wrote them. Friction choice: rewrite requirements based on your own interpretation. Better: schedule a 3-way call (BA, developer, client) to clarify intent together. crossRoleImpact: [{affectedRole: "developer", affectedPhase: "development", severity: "medium"}]. tomorrowAction: "When a developer flags vague requirements, add a 'Clarification Request Log' entry with: original text, developer's question, and schedule a 15-minute 3-way call within 48 hours."

**BA Design (data/scenarios/ba/design.ts):**
Export `baDesignScenarios` array with 4 validated scenarios:
- `ba-des-client_friction-01` — "The Wireframe Disconnect" — Client says wireframes don't match signed-off requirements. BA authored both. Friction choice: defend the wireframes as "creative interpretation." Better: create a traceability matrix mapping each wireframe element to its requirement. crossRoleImpact: [{affectedRole: "designer", affectedPhase: "design", severity: "high"}]. tomorrowAction: "Create a 'Requirements-to-Design Traceability Matrix' template that maps every wireframe screen to its source requirement — present this alongside wireframes in every review."
- `ba-des-internal_bureaucracy-01` — "The UAT Environment" — UAT environment setup requires change control board approval that takes 3 weeks. Testing window is 2 weeks away. Friction choice: wait for official approval and accept compressed UAT. Better: request parallel provisioning while CCB processes, with rollback plan. crossRoleImpact: [{affectedRole: "qa", affectedPhase: "testing", severity: "high"}]. tomorrowAction: "File UAT environment requests 6 weeks before planned UAT — add 'UAT Environment Lead Time' as a standing item in your project kickoff checklist."
- `ba-des-knowledge_gap-01` — "The Unstructured UAT" — BA facilitates UAT but testers give subjective opinions, not acceptance criteria feedback. Friction choice: accept subjective feedback and translate to bug reports yourself. Better: prepare structured UAT scripts with pass/fail criteria before the session. crossRoleImpact: [{affectedRole: "qa", affectedPhase: "testing", severity: "medium"}]. tomorrowAction: "Create a 'UAT Script Template' with: test scenario, precondition, exact steps, expected result, pass/fail checkbox — distribute to testers 48 hours before the UAT session."
- `ba-des-conflict_avoidance-01` — "The Silent Gap" — BA identifies a critical requirements gap during design review but doesn't raise it to avoid reopening sign-off. Friction choice: note the gap privately and hope it doesn't surface. Better: raise it formally with a scoped change request that doesn't reopen the entire requirements document. crossRoleImpact: [{affectedRole: "developer", affectedPhase: "development", severity: "high"}]. tomorrowAction: "When you spot a gap post-sign-off, file a 'Targeted Amendment Request' — scoped to the single gap, with impact assessment, not a full requirements reopen."

All scenarios: Indian B2B context, all debrief fields under 150 words, scenario IDs prefixed with `ba-`.
  </action>
  <verify>
    <automated>cd "/Users/shashankrai/Documents/Duplicate human-centric-lab-main 15 jan" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - data/scenarios/ba/discovery.ts contains `export const baDiscoveryScenarios`
    - data/scenarios/ba/requirements.ts contains `export const baRequirementsScenarios`
    - data/scenarios/ba/design.ts contains `export const baDesignScenarios`
    - Each file contains exactly 4 `validateScenario(` calls
    - All scenario IDs start with `ba-`
    - Every scenario has `role: "ba"`
    - Every debrief has all 5 fields: whatHappened, why, how, whoBecame, tomorrowAction
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>BA role has 12 Zod-validated scenarios across 3 SDLC phases with Indian B2B context and artifact-specific tomorrowActions.</done>
</task>

<task type="auto">
  <name>Task 2: Author CRM role SDLC scenarios (3 files, 12 scenarios)</name>
  <files>data/scenarios/crm/discovery.ts, data/scenarios/crm/requirements.ts, data/scenarios/crm/design.ts</files>
  <read_first>
    - data/scenarios/pm/discovery.ts (reference file for structure and quality bar)
    - data/scenarios/schema.ts (Zod schema)
    - data/scenarios/types.ts (type definitions)
    - data/scenarios/ba/discovery.ts (just created by Task 1 — verify pattern consistency)
  </read_first>
  <action>
Create directory `data/scenarios/crm/` and 3 scenario files.

**CRM Discovery (data/scenarios/crm/discovery.ts):**
Export `crmDiscoveryScenarios` array with 4 validated scenarios:
- `crm-discovery-client_friction-01` — "The Renewal Confrontation" — Existing client reports product friction in renewal meeting. ₹2Cr annual contract. CRM must choose between acknowledging limitations or deflecting. Friction choice: deflect product friction to preserve relationship tone. Better: acknowledge the friction, document it, and present a resolution timeline. crossRoleImpact: [{affectedRole: "pm", affectedPhase: "requirements", severity: "high", description: "PM never receives the client friction data — the same issue reappears in the next quarter with higher severity"}]. tomorrowAction: "Add a 'Client Friction Log' section to your QBR template — every friction reported gets documented with: issue, impact on client workflow, resolution owner, target date."
- `crm-discovery-internal_bureaucracy-01` — "The Escalation Maze" — Client escalation requires coordinating across 4 internal teams (PM, Support, Sales, Finance). No single owner. ₹3Cr account. Friction choice: send separate emails to each team and wait for responses. Better: convene a 30-minute war room with all 4 teams and establish a single escalation owner. crossRoleImpact: [{affectedRole: "sales", affectedPhase: "discovery", severity: "medium", description: "Sales loses visibility on account health — enters next renewal conversation without context on unresolved escalation"}]. tomorrowAction: "Create an 'Escalation War Room Protocol' document: trigger criteria, mandatory attendees per escalation tier, 30-minute time limit, single owner assigned before the call ends."
- `crm-discovery-knowledge_gap-01` — "The Undocumented History" — New CRM manager inherits an account with 18 months of undocumented relationship history. Renewal meeting in 2 weeks. ₹4Cr account. Friction choice: prepare for the renewal based on billing and contract data only. Better: schedule calls with previous CRM, support lead, and delivery PM to reconstruct relationship context. crossRoleImpact: [{affectedRole: "sales", affectedPhase: "requirements", severity: "medium", description: "Sales has no account health baseline — upsell or expansion conversations start without understanding current satisfaction level"}]. tomorrowAction: "Create an 'Account Handover Checklist' covering: relationship history summary, key stakeholder map, unresolved issues, satisfaction trajectory, and verbal commitments made."
- `crm-discovery-conflict_avoidance-01` — "The Optimistic Report" — Client is unhappy with delivery outcome but CRM has been telling leadership everything is on track. ₹2.5Cr account. Friction choice: continue the optimistic narrative and smooth over the issue. Better: escalate the delivery gap to leadership with a corrective action plan. crossRoleImpact: [{affectedRole: "pm", affectedPhase: "design", severity: "high", description: "PM is blindsided when client raises delivery issues in a steering committee meeting — no corrective action was planned"}]. tomorrowAction: "Add a 'Client Health Reality Check' to your monthly account review: compare your internal narrative with the client's last 3 support tickets and NPS response."

**CRM Requirements (data/scenarios/crm/requirements.ts):**
Export `crmRequirementsScenarios` array with 4 validated scenarios:
- `crm-req-client_friction-01` — "The Roadmap Gap" — During QBR, client asks for features 6 months away. ₹3Cr renewal at stake. Friction choice: hint that features are "coming soon" without committing a timeline. Better: share the roadmap timeline transparently and propose interim workarounds. crossRoleImpact: [{affectedRole: "pm", affectedPhase: "requirements", severity: "medium"}]. tomorrowAction: "Create a 'Feature Timeline Communication Script' with 3 templates: available now, roadmap with ETA, and not planned — use in every QBR where clients ask about futures."
- `crm-req-internal_bureaucracy-01` — "The Data Silo" — Account health review requires data from 5 disconnected systems. No integration. Friction choice: present partial data from the one system you have access to. Better: manually compile data from all 5 sources into a single health dashboard for this review. crossRoleImpact: [{affectedRole: "strategy", affectedPhase: "discovery", severity: "medium"}]. tomorrowAction: "Build a 'Manual Account Health Dashboard' spreadsheet pulling from: CRM notes, billing system, support tickets, NPS scores, usage analytics — update monthly until systems are integrated."
- `crm-req-knowledge_gap-01` — "The Invisible Friction" — Client reports a recurring friction point CRM hasn't noticed because the metric wasn't tracked. Friction choice: acknowledge the issue reactively without investigating history. Better: audit support tickets and usage data for the last 6 months to quantify the friction pattern. crossRoleImpact: [{affectedRole: "qa", affectedPhase: "testing", severity: "medium"}]. tomorrowAction: "Add a 'Friction Pattern Audit' to your quarterly account review: pull the top 5 support ticket categories and check if any correlate with client-reported friction."
- `crm-req-conflict_avoidance-01` — "The Milestone Miss" — Delivery team missed a milestone. CRM must tell the client while managing leadership expectations. Friction choice: delay telling the client while trying to fix internally. Better: proactively communicate the delay with a recovery plan before the client notices. crossRoleImpact: [{affectedRole: "pm", affectedPhase: "design", severity: "medium"}]. tomorrowAction: "Create a 'Delivery Delay Communication Template' with: what happened, impact on timeline, recovery plan, and compensation offer (if applicable) — send within 24 hours of any milestone miss."

**CRM Design (data/scenarios/crm/design.ts):**
Export `crmDesignScenarios` array with 4 validated scenarios:
- `crm-des-client_friction-01` — "The Custom Onboarding Promise" — Client wants custom onboarding flow. CRM promised it but product investment not approved. ₹2Cr account. Friction choice: tell the client it's "in progress" while hoping product approves it. Better: present a phased onboarding plan starting with what's available today. crossRoleImpact: [{affectedRole: "pm", affectedPhase: "requirements", severity: "high"}]. tomorrowAction: "Before promising custom features, add a 'Product Investment Check' step to your account planning — confirm with PM that the feature has approved engineering capacity before committing to the client."
- `crm-des-internal_bureaucracy-01` — "The Renewal Delay" — Renewal contract needs legal review delaying past fiscal year end. Both parties lose. Friction choice: accept the legal timeline and risk lapse. Better: flag to legal with fiscal year deadline and request expedited review. crossRoleImpact: [{affectedRole: "sales", affectedPhase: "requirements", severity: "high"}]. tomorrowAction: "Start renewal legal review 90 days before contract end — add 'Legal Review Lead Time' as a calendar reminder for every account with a renewal in the next quarter."
- `crm-des-knowledge_gap-01` — "The Champion Exit" — Client's internal champion leaves. New stakeholder has different expectations. No documented success metrics. Friction choice: introduce yourself to the new stakeholder without preparation. Better: compile a success metrics summary from the original deal before the first meeting. crossRoleImpact: [{affectedRole: "sales", affectedPhase: "discovery", severity: "high"}]. tomorrowAction: "For every account, maintain a 'Champion Continuity Document' with: original business case, measured success metrics, and stakeholder-specific value propositions — update quarterly."
- `crm-des-conflict_avoidance-01` — "The Gift Voucher Fix" — Client satisfaction drops. CRM smooths it over with a gift voucher instead of investigating root cause. Friction choice: send a goodwill gesture without root cause analysis. Better: conduct a root cause investigation and present findings with a corrective action plan. crossRoleImpact: [{affectedRole: "pm", affectedPhase: "design", severity: "medium"}]. tomorrowAction: "When satisfaction drops, complete a 'Root Cause Analysis Brief' before any goodwill gesture — identify: trigger event, systemic vs one-time, fix owner, and prevention plan."

All scenarios: Indian B2B context, all debrief fields under 150 words, scenario IDs prefixed with `crm-`.
  </action>
  <verify>
    <automated>cd "/Users/shashankrai/Documents/Duplicate human-centric-lab-main 15 jan" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - data/scenarios/crm/discovery.ts contains `export const crmDiscoveryScenarios`
    - data/scenarios/crm/requirements.ts contains `export const crmRequirementsScenarios`
    - data/scenarios/crm/design.ts contains `export const crmDesignScenarios`
    - Each file contains exactly 4 `validateScenario(` calls
    - All scenario IDs start with `crm-`
    - Every scenario has `role: "crm"`
    - Every debrief has all 5 fields: whatHappened, why, how, whoBecame, tomorrowAction
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>CRM role has 12 Zod-validated scenarios across 3 SDLC phases with Indian B2B context and artifact-specific tomorrowActions.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Each of the 6 files exports a named array of exactly 4 ScenarioConfig objects
- `npm run build` succeeds
</verification>

<success_criteria>
24 scenarios created (12 BA + 12 CRM), all Zod-validated, all with Indian B2B context, all with crossRoleImpact arrays, all with artifact-specific tomorrowActions under 150 words.
</success_criteria>

<output>
After completion, create `.planning/phases/05-full-platform-delivery/05-02-SUMMARY.md`
</output>
