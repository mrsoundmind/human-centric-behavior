---
phase: 05-full-platform-delivery
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/experiences/client/primer/ClientFinalConnect.tsx
  - src/components/experiences/client/ClientExperience.tsx
autonomous: true
requirements: [AUDT-01, AUDT-02]

must_haves:
  truths:
    - "ClientFinalConnect has a bridge CTA linking to /internalteam"
    - "All 18 client experience screens have a clear next action with no dead ends"
    - "All 5 funnel friction experiences have visible good counterparts"
  artifacts:
    - path: "src/components/experiences/client/primer/ClientFinalConnect.tsx"
      provides: "Bridge CTA to internal team experience"
      contains: "Link to=\"/internalteam\""
  key_links:
    - from: "src/components/experiences/client/primer/ClientFinalConnect.tsx"
      to: "/internalteam route"
      via: "React Router Link"
      pattern: "to=\"/internalteam\""
---

<objective>
Audit and fix the client-facing experience flow: add bridge CTA from ClientFinalConnect to /internalteam, verify all 18 screens for navigation continuity, and confirm all 5 funnel bad/good pairs are complete.

Purpose: The client experience currently dead-ends at ClientFinalConnect with only a "Restart" button and external "Let's Connect" link. Users need a pathway to the internal team experience. The funnel friction pairs need visual and content consistency verification.

Output: ClientFinalConnect with /internalteam bridge, verified navigation continuity across all 18 client screens.
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-full-platform-delivery/05-CONTEXT.md
@.planning/phases/05-full-platform-delivery/05-RESEARCH.md

<interfaces>
From src/components/experiences/client/ClientExperience.tsx (18 steps confirmed):
```typescript
// Steps array in order — all 18 screens
// Primer: ClientPrimerDoor, ClientPrimerStove, ClientPrimerKetchup
// Funnel: FunnelBait, BaitGood, FunnelChaos, ChaosGood, FunnelWall, WallGood, FunnelMaze, MazeGood, FunnelTrap, TrapGood
// Impact: RevenueImpact section screens
// Final: ClientFinalConnect
```

From src/components/experiences/client/primer/ClientFinalConnect.tsx (current):
```typescript
interface ClientFinalConnectProps {
    onRestart: () => void;
}
// Has: "Let's Connect" external link + "Replay Experience" button
// Missing: Bridge to /internalteam
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /internalteam bridge CTA to ClientFinalConnect</name>
  <files>src/components/experiences/client/primer/ClientFinalConnect.tsx</files>
  <read_first>
    - src/components/experiences/client/primer/ClientFinalConnect.tsx (current state — 50 lines)
    - src/App.tsx (verify /internalteam route exists)
  </read_first>
  <action>
Add a React Router `Link` component to ClientFinalConnect.tsx that bridges to the internal team experience.

1. Add import: `import { Link } from "react-router-dom";`

2. In the button group (the `flex flex-col md:flex-row` div around the "Let's Connect" and "Replay Experience" elements), add a third CTA between the existing two buttons:

```tsx
<Link
  to="/internalteam"
  className="group relative inline-flex items-center gap-3 px-12 py-5 bg-transparent border border-white/20 text-white rounded-full font-bold text-xl hover:border-white/60 transition-all"
>
  <span>See how your team navigates these frictions</span>
  <span className="group-hover:translate-x-1 transition-transform">→</span>
</Link>
```

3. Do NOT modify the `onRestart` prop, the interface, the existing "Let's Connect" link, or the "Replay Experience" button.
4. Do NOT use `window.location.href` — use React Router's `Link` component.
5. The button style uses `bg-transparent border border-white/20` to differentiate from the primary "Let's Connect" CTA which uses `bg-white text-black`.
  </action>
  <verify>
    <automated>cd "/Users/shashankrai/Documents/Duplicate human-centric-lab-main 15 jan" && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - ClientFinalConnect.tsx contains `import { Link } from "react-router-dom"`
    - ClientFinalConnect.tsx contains `to="/internalteam"`
    - ClientFinalConnect.tsx contains `See how your team navigates these frictions`
    - ClientFinalConnect.tsx still contains `onRestart` prop usage
    - ClientFinalConnect.tsx still contains `Let's Connect` text
    - ClientFinalConnect.tsx still contains `Replay Experience` text
    - ClientFinalConnectProps interface is unchanged (still only `onRestart: () => void`)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ClientFinalConnect has a bridge CTA to /internalteam alongside existing buttons, using React Router Link.</done>
</task>

<task type="auto">
  <name>Task 2: Audit client experience navigation continuity and funnel pairs</name>
  <files>src/components/experiences/client/ClientExperience.tsx</files>
  <read_first>
    - src/components/experiences/client/ClientExperience.tsx (full file — orchestrator with all 18 steps)
    - src/components/experiences/client/primer/ClientFinalConnect.tsx (just updated)
    - src/components/experiences/client/funnel/FunnelBait.tsx (sample funnel bad screen)
    - src/components/experiences/client/funnel/BaitGood.tsx (sample funnel good screen)
  </read_first>
  <action>
Audit all 18 client experience screens for navigation continuity. This is a read-and-verify task with targeted fixes if dead ends are found.

**Step 1: Verify all 5 funnel bad/good pairs are imported and rendered in ClientExperience.tsx:**
- FunnelBait / BaitGood
- FunnelChaos / ChaosGood
- FunnelWall / WallGood
- FunnelMaze / MazeGood
- FunnelTrap / TrapGood

All 10 components must be imported and each pair must appear consecutively in the steps array (bad then good).

**Step 2: Verify navigation continuity:**
- Every step in the steps array must have an `onNext` or equivalent prop that advances to the next step
- The final step (ClientFinalConnect) must have the bridge CTA (verified by Task 1)
- No step should render without a clear "next" action visible to the user

**Step 3: If any dead ends are found** (a screen with no next action), fix by:
- Adding `onNext` prop pass-through if the component supports it
- Or adding a continue button to the component if it lacks one

**Step 4: Verify that the steps array order creates a logical narrative flow:**
Primer (physical world) → Funnel (digital misapplication) → Impact (revenue) → Final Connect

If all checks pass and no dead ends found, document the audit result in the summary. If fixes are needed, implement them directly.

Note: Do NOT modify any funnel component's internal content — only fix navigation wiring if broken.
  </action>
  <verify>
    <automated>cd "/Users/shashankrai/Documents/Duplicate human-centric-lab-main 15 jan" && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - ClientExperience.tsx imports all 10 funnel components (5 bad + 5 good): FunnelBait, BaitGood, FunnelChaos, ChaosGood, FunnelWall, WallGood, FunnelMaze, MazeGood, FunnelTrap, TrapGood
    - ClientExperience.tsx has 18 steps in the steps/screens array
    - Every step has a navigation mechanism (onNext, onClick, or Link)
    - `npx tsc --noEmit` exits 0
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>All 18 client experience screens audited for navigation continuity. All 5 funnel bad/good pairs confirmed present and ordered. No dead ends remain.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds
- ClientFinalConnect has 3 CTAs: "Let's Connect" (external), bridge to /internalteam, "Replay Experience"
- All 18 client screens have navigation continuity
</verification>

<success_criteria>
Client experience flow has no dead ends. ClientFinalConnect bridges to /internalteam. All 5 funnel friction/good pairs are complete and visually paired.
</success_criteria>

<output>
After completion, create `.planning/phases/05-full-platform-delivery/05-04-SUMMARY.md`
</output>
