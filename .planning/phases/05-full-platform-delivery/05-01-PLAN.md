---
phase: 05-full-platform-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/scenarios/sales/discovery.ts
  - data/scenarios/sales/requirements.ts
  - data/scenarios/sales/design.ts
  - data/scenarios/designer/discovery.ts
  - data/scenarios/designer/requirements.ts
  - data/scenarios/designer/design.ts
autonomous: true
requirements: [AUDT-01]

must_haves:
  truths:
    - "Sales role has 12 scenarios covering Discovery, Requirements, Design phases with all 4 friction dimensions"
    - "Designer role has 12 scenarios covering Discovery, Requirements, Design phases with all 4 friction dimensions"
    - "All scenarios pass Zod validation at import time without errors"
    - "Every tomorrowAction names a specific workflow artifact, not generic advice"
  artifacts:
    - path: "data/scenarios/sales/discovery.ts"
      provides: "4 Sales Discovery scenarios"
      exports: ["salesDiscoveryScenarios"]
    - path: "data/scenarios/sales/requirements.ts"
      provides: "4 Sales Requirements scenarios"
      exports: ["salesRequirementsScenarios"]
    - path: "data/scenarios/sales/design.ts"
      provides: "4 Sales Design scenarios"
      exports: ["salesDesignScenarios"]
    - path: "data/scenarios/designer/discovery.ts"
      provides: "4 Designer Discovery scenarios"
      exports: ["designerDiscoveryScenarios"]
    - path: "data/scenarios/designer/requirements.ts"
      provides: "4 Designer Requirements scenarios"
      exports: ["designerRequirementsScenarios"]
    - path: "data/scenarios/designer/design.ts"
      provides: "4 Designer Design scenarios"
      exports: ["designerDesignScenarios"]
  key_links:
    - from: "data/scenarios/sales/*.ts"
      to: "data/scenarios/schema.ts"
      via: "validateScenario import"
      pattern: "import.*validateScenario.*from.*schema"
    - from: "data/scenarios/designer/*.ts"
      to: "data/scenarios/schema.ts"
      via: "validateScenario import"
      pattern: "import.*validateScenario.*from.*schema"
---

<objective>
Author 24 SDLC-merged scenarios for Sales and Designer roles (12 each: 3 phases x 4 friction dimensions).

Purpose: Sales and Designer are 2 of the 5 remaining roles that need full SDLC journeys to complete the platform. Each role gets scenarios covering client_friction, internal_bureaucracy, knowledge_gap, and conflict_avoidance in Discovery, Requirements, and Design phases.

Output: 6 scenario data files (3 per role) with Zod-validated ScenarioConfig objects, Indian B2B context, crossRoleImpact arrays, and artifact-specific tomorrowAction fields.
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-full-platform-delivery/05-CONTEXT.md
@.planning/phases/05-full-platform-delivery/05-RESEARCH.md

<interfaces>
<!-- Reference scenario file structure — replicate exactly -->
From data/scenarios/pm/discovery.ts:
```typescript
import { validateScenario } from "../schema";

const rawClientFriction = {
  id: "pm-discovery-client-01",
  role: "pm",
  phase: "discovery",
  frictionDimension: "client_friction",
  title: "The Undocumented Assumption",
  setup: "A ₹3Cr IBC client wants...",
  choices: [
    {
      id: "pm-disc-c01-a",
      text: "The friction-causing choice",
      frictionTag: "conflict_avoidance",
      crossRoleImpact: [
        { affectedRole: "ba", affectedPhase: "requirements", description: "...", severity: "high" }
      ],
    },
    {
      id: "pm-disc-c01-b",
      text: "The better choice",
      frictionTag: "client_friction",
      crossRoleImpact: [],
    },
  ],
  debrief: {
    whatHappened: "...",
    why: "...",
    how: "...",
    whoBecame: "...",
    tomorrowAction: "In your next [specific moment], [specific artifact] + [exact action].",
  },
};

export const pmDiscoveryScenarios = [
  validateScenario(rawClientFriction, "data/scenarios/pm/discovery.ts"),
  validateScenario(rawInternalBureaucracy, "data/scenarios/pm/discovery.ts"),
  validateScenario(rawKnowledgeGap, "data/scenarios/pm/discovery.ts"),
  validateScenario(rawConflictAvoidance, "data/scenarios/pm/discovery.ts"),
];
```

From data/scenarios/types.ts:
```typescript
type Designation = "sales" | "crm" | "pm" | "developer" | "qa" | "designer" | "strategy" | "ba";
type SDLCPhase = "discovery" | "requirements" | "design" | "development" | "testing" | "launch" | "maintenance";
type FrictionDimension = "client_friction" | "internal_bureaucracy" | "knowledge_gap" | "conflict_avoidance";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Sales role SDLC scenarios (3 files, 12 scenarios)</name>
  <files>data/scenarios/sales/discovery.ts, data/scenarios/sales/requirements.ts, data/scenarios/sales/design.ts</files>
  <read_first>
    - data/scenarios/pm/discovery.ts (reference file for structure and quality bar)
    - data/scenarios/pm/requirements.ts (reference for requirements phase pattern)
    - data/scenarios/pm/design.ts (reference for design phase pattern)
    - data/scenarios/schema.ts (Zod schema — source of truth for validation)
    - data/scenarios/types.ts (type definitions for Designation, SDLCPhase, FrictionDimension)
  </read_first>
  <action>
Create directory `data/scenarios/sales/` and 3 scenario files following the exact PM reference pattern.

**Sales Discovery (data/scenarios/sales/discovery.ts):**
Export `salesDiscoveryScenarios` array with 4 validated scenarios:
- `sales-discovery-client_friction-01` — "The Verbal Scope" — ₹2Cr IBC deal, prospect says "we want everything from the demo" but no written scope. Sales wants to close before quarter-end. Friction choice: send proposal based on demo walkthrough notes alone. Better: request written scope confirmation before proposal. crossRoleImpact: [{affectedRole: "ba", affectedPhase: "requirements", severity: "high", description: "BA inherits verbal scope as the only requirements source — must reconstruct what was actually promised"}]. tomorrowAction: "Before your next proposal, add a 'Scope Confirmation Appendix' to your proposal template listing every feature mentioned verbally with client's written acknowledgment."
- `sales-discovery-internal_bureaucracy-01` — "The Approval Bottleneck" — ₹5Cr enterprise contract needs legal review, competitor has 1-week SLA. Friction choice: wait for full legal cycle. Better: request expedited review with risk summary. crossRoleImpact: [{affectedRole: "pm", affectedPhase: "discovery", severity: "medium", description: "PM's project timeline starts 3 weeks late — discovery phase compressed"}]. tomorrowAction: "Create a 'Fast-Track Legal Request' template with pre-filled risk categories for deals above ₹3Cr with competitive timelines."
- `sales-discovery-knowledge_gap-01` — "The Integration Blind Spot" — live demo with client's CTO asking about SAP compatibility, sales rep doesn't know platform's integration limitations. Friction choice: say "yes, we support that" without verification. Better: say "let me get our technical architect to confirm the exact integration scope." crossRoleImpact: [{affectedRole: "developer", affectedPhase: "development", severity: "high", description: "Developer discovers promised SAP integration requires 6 weeks of custom connector work not in the estimate"}]. tomorrowAction: "Build a 'Technical Capability Matrix' one-pager listing confirmed integrations, conditional integrations, and unsupported platforms. Carry it to every demo."
- `sales-discovery-conflict_avoidance-01` — "The Price Absorber" — ₹4Cr deal, procurement head drops price by 30% in final call. Sales rep is over-target. Friction choice: accept the discount to close. Better: counter with value breakdown and scope adjustment. crossRoleImpact: [{affectedRole: "pm", affectedPhase: "requirements", severity: "high", description: "PM must deliver ₹4Cr scope on ₹2.8Cr budget — every requirements trade-off is constrained by the margin gap"}]. tomorrowAction: "Create a 'Value Defense Script' document with 3 pre-written responses for discount requests above 15%, each tied to specific scope trade-offs."

**Sales Requirements (data/scenarios/sales/requirements.ts):**
Export `salesRequirementsScenarios` array with 4 validated scenarios:
- `sales-req-client_friction-01` — "The Scope Creep Handshake" — Signed SoW says "standard reporting" but client interprets as custom dashboards. Friction choice: agree to the expanded interpretation to preserve relationship. Better: schedule a scope alignment call referencing exact SoW language. crossRoleImpact: [{affectedRole: "developer", affectedPhase: "development", severity: "high"}]. tomorrowAction: "Add a 'Feature Definition Glossary' appendix to your SoW template defining every feature term with concrete deliverable boundaries."
- `sales-req-internal_bureaucracy-01` — "The Resource Queue" — Pre-sales engineer needed for scoping is fully allocated 3 weeks. Friction choice: proceed without technical scoping. Better: escalate resource conflict with business impact memo. crossRoleImpact: [{affectedRole: "ba", affectedPhase: "requirements", severity: "medium"}]. tomorrowAction: "Draft a 'Resource Escalation Template' that quantifies deal revenue at risk per day of scoping delay."
- `sales-req-knowledge_gap-01` — "The Lost Handover" — Handover from pre-sales to delivery PM loses critical context about non-negotiable client constraints. Friction choice: send a forwarded email chain as handover. Better: conduct a structured 30-minute handover meeting with a checklist. crossRoleImpact: [{affectedRole: "pm", affectedPhase: "discovery", severity: "high"}]. tomorrowAction: "Create a 'Pre-Sales to Delivery Handover Checklist' covering: non-negotiable constraints, verbal promises, competitor context, stakeholder personalities."
- `sales-req-conflict_avoidance-01` — "The SLA Absorber" — Client's legal adds 100% uptime SLA clause. Sales absorbs without flagging to delivery. Friction choice: sign the clause to close faster. Better: flag to delivery team and negotiate realistic SLA tiers. crossRoleImpact: [{affectedRole: "developer", affectedPhase: "development", severity: "high"}]. tomorrowAction: "Add an 'SLA Review Gate' to your contract sign-off checklist — no SLA above 99.5% without written delivery team confirmation."

**Sales Design (data/scenarios/sales/design.ts):**
Export `salesDesignScenarios` array with 4 validated scenarios:
- `sales-des-client_friction-01` — "The Brand Promise" — Client requests brand-specific UI theme deviating from design system, sales already promised. Friction choice: confirm to client that custom theme is standard scope. Better: loop in designer to assess effort and set expectations. crossRoleImpact: [{affectedRole: "designer", affectedPhase: "design", severity: "high"}]. tomorrowAction: "Add a 'Design Customization Scope' clause to your proposal template with 3 tiers: standard theme, branded theme (+20% effort), fully custom (+40% effort)."
- `sales-des-internal_bureaucracy-01` — "The Sign-Off Maze" — Design review requires 3 stakeholder sign-offs across time zones. Friction choice: wait for sequential approvals. Better: set up a single async review window with a 48-hour deadline. crossRoleImpact: [{affectedRole: "designer", affectedPhase: "design", severity: "medium"}]. tomorrowAction: "Propose a 'Parallel Review Protocol' document to your delivery lead — all stakeholders review simultaneously with a 48-hour response SLA."
- `sales-des-knowledge_gap-01` — "The Phantom Feature" — Demo showed prototype features not in current roadmap. Friction choice: avoid correcting the client's expectations. Better: send a feature availability matrix post-demo. crossRoleImpact: [{affectedRole: "pm", affectedPhase: "requirements", severity: "high"}]. tomorrowAction: "After every demo, send a 'Feature Availability Confirmation' email listing: available now, roadmap (with timeline), and not planned."
- `sales-des-conflict_avoidance-01` — "The Vague Pushback" — Client says wireframes "don't look right" — vague feedback. Sales deflects instead of diagnosing. Friction choice: tell the client "we'll fix it" without specifics. Better: facilitate a structured feedback session to identify exact concerns. crossRoleImpact: [{affectedRole: "designer", affectedPhase: "design", severity: "medium"}]. tomorrowAction: "Create a 'Design Feedback Facilitation Guide' with 5 probing questions to convert vague client reactions into actionable design direction."

All scenarios must use Indian B2B context (IBC, SAP, TCS, enterprise procurement, rupee amounts). All debrief fields must be under 150 words each. Each crossRoleImpact description must specify the downstream effect concretely.
  </action>
  <verify>
    <automated>cd "/Users/shashankrai/Documents/Duplicate human-centric-lab-main 15 jan" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - data/scenarios/sales/discovery.ts contains `export const salesDiscoveryScenarios`
    - data/scenarios/sales/requirements.ts contains `export const salesRequirementsScenarios`
    - data/scenarios/sales/design.ts contains `export const salesDesignScenarios`
    - Each file contains exactly 4 `validateScenario(` calls
    - All scenario IDs start with `sales-`
    - Every scenario has `role: "sales"`
    - Every debrief has all 5 fields: whatHappened, why, how, whoBecame, tomorrowAction
    - Every tomorrowAction references a specific artifact name (template, checklist, matrix, guide, etc.)
    - Every friction choice has at least one crossRoleImpact entry with affectedRole, affectedPhase, description, severity
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Sales role has 12 Zod-validated scenarios across 3 SDLC phases, all with Indian B2B context, artifact-specific tomorrowActions, and crossRoleImpact arrays.</done>
</task>

<task type="auto">
  <name>Task 2: Author Designer role SDLC scenarios (3 files, 12 scenarios)</name>
  <files>data/scenarios/designer/discovery.ts, data/scenarios/designer/requirements.ts, data/scenarios/designer/design.ts</files>
  <read_first>
    - data/scenarios/pm/discovery.ts (reference file for structure and quality bar)
    - data/scenarios/schema.ts (Zod schema)
    - data/scenarios/types.ts (type definitions)
    - data/scenarios/sales/discovery.ts (just created by Task 1 — verify pattern consistency)
  </read_first>
  <action>
Create directory `data/scenarios/designer/` and 3 scenario files following the exact PM reference pattern.

**Designer Discovery (data/scenarios/designer/discovery.ts):**
Export `designerDiscoveryScenarios` array with 4 validated scenarios:
- `designer-discovery-client_friction-01` — "The Premature Concept" — Client wants to see design concepts before discovery is complete. ₹2.5Cr TCS project, stakeholder says "just show us something to get excited." Friction choice: create quick mockups from incomplete discovery notes. Better: share a discovery timeline showing when informed concepts will be ready. crossRoleImpact: [{affectedRole: "ba", affectedPhase: "requirements", severity: "medium", description: "BA's requirement gathering is undermined — client references premature mockups as 'agreed design' during requirements"}]. tomorrowAction: "Add a 'Design Readiness Gate' slide to your discovery deck showing: discovery complete -> user research synthesized -> concepts begin. No concepts before this gate."
- `designer-discovery-internal_bureaucracy-01` — "The Tool Procurement Delay" — Figma org seat requires IT procurement approval — 2-week delay before designer can start. ₹3Cr project, sprint planning in 5 days. Friction choice: wait for official tool access. Better: request emergency access while procurement processes in parallel. crossRoleImpact: [{affectedRole: "pm", affectedPhase: "discovery", severity: "medium", description: "PM's discovery timeline absorbs 2-week tool procurement delay — compresses all downstream phases"}]. tomorrowAction: "Maintain a 'Design Tool Emergency Access Request' template pre-approved by IT for projects above ₹2Cr — submit on day 1, not day 5."
- `designer-discovery-knowledge_gap-01` — "The Missing Context" — Designer joins mid-project with no access to original user research or decision rationale. ₹4Cr IBC migration. Friction choice: start designing based on the requirements doc alone. Better: request a 1-hour context transfer session with the original researcher before any design work. crossRoleImpact: [{affectedRole: "developer", affectedPhase: "development", severity: "medium", description: "Developer builds against designs that contradict earlier user research findings — rework surfaces in QA"}]. tomorrowAction: "Create a 'Design Context Onboarding Checklist' for mid-project joins: user research summary, decision log, stakeholder map, design system status, known constraints."
- `designer-discovery-conflict_avoidance-01` — "The Pattern Compliance" — Stakeholder insists on a specific UI pattern the designer knows creates friction — a mega-dropdown navigation. Friction choice: implement the stakeholder's preferred pattern without pushback. Better: present evidence from comparable projects showing the friction cost of mega-dropdowns. crossRoleImpact: [{affectedRole: "qa", affectedPhase: "testing", severity: "medium", description: "QA must test a known-problematic interaction pattern — usability bugs are predictable but must still be documented"}]. tomorrowAction: "Build a 'Pattern Evidence Library' in your design system wiki with 3-5 case studies per anti-pattern showing measured user friction (bounce rates, task completion times)."

**Designer Requirements (data/scenarios/designer/requirements.ts):**
Export `designerRequirementsScenarios` array with 4 validated scenarios:
- `designer-req-client_friction-01` — "The Legal Copy" — Requirements doc uses legal language for user-facing error messages. Friction choice: implement legal text verbatim as UX copy. Better: propose plain-language alternatives with legal review. crossRoleImpact: [{affectedRole: "developer", affectedPhase: "development", severity: "low"}]. tomorrowAction: "Create a 'UX Copy Translation Template' mapping legal requirements to plain-language alternatives for the 10 most common error states."
- `designer-req-internal_bureaucracy-01` — "The Review Backlog" — Design system update requires central platform team approval with 6-week review backlog. Friction choice: wait for platform team approval. Better: submit with expedite flag and build with current system, plan migration. crossRoleImpact: [{affectedRole: "developer", affectedPhase: "development", severity: "medium"}]. tomorrowAction: "Draft a 'Design System Exception Request' template that documents: component needed, why existing won't work, risk of delay, proposed interim solution."
- `designer-req-knowledge_gap-01` — "The Contextless Spec" — Requirements doc has no user personas, no usage context, no priority indicators. Friction choice: design based on assumptions and personal experience. Better: request a 45-minute persona briefing before starting design. crossRoleImpact: [{affectedRole: "qa", affectedPhase: "testing", severity: "medium"}]. tomorrowAction: "Add a 'Design Input Checklist' to your sprint intake: personas attached? Usage context documented? Priority indicators present? Block design start until all 3 are answered."
- `designer-req-conflict_avoidance-01` — "The Dual Design" — PM and developer disagree on interaction model. Designer absorbs ambiguity and designs both options. Friction choice: design both options to avoid choosing sides. Better: facilitate a 30-minute alignment session to resolve before designing either. crossRoleImpact: [{affectedRole: "developer", affectedPhase: "development", severity: "high"}]. tomorrowAction: "When you receive conflicting direction, send a 'Design Direction Alignment Request' email with both options side-by-side and request a single decision before design begins."

**Designer Design (data/scenarios/designer/design.ts):**
Export `designerDesignScenarios` array with 4 validated scenarios:
- `designer-des-client_friction-01` — "The Accessibility Conflict" — Client brand preferences conflict with WCAG accessibility. ₹3Cr project. Friction choice: prioritize brand over accessibility. Better: present accessible alternatives that maintain brand identity. crossRoleImpact: [{affectedRole: "qa", affectedPhase: "testing", severity: "high"}]. tomorrowAction: "Create an 'Accessibility-Brand Compatibility Matrix' for your design system showing: brand colors -> nearest WCAG AA compliant alternatives."
- `designer-des-internal_bureaucracy-01` — "The Committee Review" — Every design decision requires 8-stakeholder review meeting, only 3 relevant. Friction choice: present to the full committee and accept consensus-driven mediocrity. Better: propose a RACI-based review where only relevant stakeholders have approval authority. crossRoleImpact: [{affectedRole: "pm", affectedPhase: "requirements", severity: "low"}]. tomorrowAction: "Draft a 'Design Review RACI' document for your project listing who Approves, who is Consulted, and who is Informed for each design category (layout, interaction, content, visual)."
- `designer-des-knowledge_gap-01` — "The Silent Handoff" — Handoff to developer lacks interaction notes — developer implements static design without micro-interactions. Friction choice: hand off Figma file without interaction documentation. Better: create an interaction spec document alongside the visual design. crossRoleImpact: [{affectedRole: "developer", affectedPhase: "development", severity: "high"}]. tomorrowAction: "Add an 'Interaction Spec Sheet' to every Figma handoff covering: hover states, transitions, loading states, error states, empty states, and edge cases."
- `designer-des-conflict_avoidance-01` — "The Effort Retreat" — Developer says animation will take 3 weeks. Designer removes it without discussing alternatives. Friction choice: remove the animation entirely to avoid conflict. Better: propose 2-3 lighter alternatives and discuss effort-impact trade-offs. crossRoleImpact: [{affectedRole: "qa", affectedPhase: "testing", severity: "low"}]. tomorrowAction: "When a design element is flagged as high-effort, prepare an 'Effort-Impact Options Brief' with 3 tiers: full implementation, reduced scope, minimal viable version."

All scenarios must use Indian B2B context. All debrief fields under 150 words. Scenario IDs prefixed with `designer-`.
  </action>
  <verify>
    <automated>cd "/Users/shashankrai/Documents/Duplicate human-centric-lab-main 15 jan" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - data/scenarios/designer/discovery.ts contains `export const designerDiscoveryScenarios`
    - data/scenarios/designer/requirements.ts contains `export const designerRequirementsScenarios`
    - data/scenarios/designer/design.ts contains `export const designerDesignScenarios`
    - Each file contains exactly 4 `validateScenario(` calls
    - All scenario IDs start with `designer-`
    - Every scenario has `role: "designer"`
    - Every debrief has all 5 fields: whatHappened, why, how, whoBecame, tomorrowAction
    - Every tomorrowAction references a specific artifact name
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Designer role has 12 Zod-validated scenarios across 3 SDLC phases, all with Indian B2B context, artifact-specific tomorrowActions, and crossRoleImpact arrays.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Each of the 6 files exports a named array of exactly 4 ScenarioConfig objects
- `npm run build` succeeds (Zod validation runs at import time during build)
</verification>

<success_criteria>
24 scenarios created (12 Sales + 12 Designer), all Zod-validated, all with Indian B2B context, all with crossRoleImpact arrays, all with artifact-specific tomorrowActions under 150 words.
</success_criteria>

<output>
After completion, create `.planning/phases/05-full-platform-delivery/05-01-SUMMARY.md`
</output>
